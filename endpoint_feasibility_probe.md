# NEXON Open API — endpoint feasibility probe 기록

작성일: 2026-09-03 · 코드: `scratchpad/step2_probe.py` (일회성, 약 190콜) · Part 2 패널 설계 근거

과거 시즌(2025~2026) longitudinal 패널을 설계하기 전에, 캐릭터 endpoint의 **소급 조회 가능
범위**와 **응답 상태 구분**을 5개 캐릭터(각 코호트 1명)로 실측했다.

---

## 1. 핵심 결론

- **`/ranking/overall` 의 "약 4개월" 소급 제한은 랭킹 전용이다.** 캐릭터 endpoint에는 적용되지 않는다.
- **`basic` / `stat` / `hexamatrix` / `symbol-equipment` / `item-equipment` / `user/union` 은
  2023-12-22 까지 소급 확인.** (2023-12-21 은 깨짐 — API 최초 데이터 경계로 추정, 하루만 무효)
- **`hexamatrix-stat` 만 2025-06-19 이후.** 그 이전은 HTTP 200 + `list[0]` (기능 미출시).
- **최신 조회 한계는 T-1.** 오늘(2026-09-03) 호출 시 `2026-09-02` OK, `2026-09-03` → HTTP 400.
- 과거 날짜도 **일단위 해상도**가 있다 (2025-06-19~22 로 `exp_rate` 일별 변동 확인).
- `live_` 키에서는 probe 전 구간 **429(레이트리밋) 미발생**.

---

## 2. 응답 상태 3종 구분 (수집 스크립트가 반드시 구분해야 함)

| 상황 | HTTP | 본문 | 판정 |
|---|---|---|---|
| 정상 소급 데이터 | 200 | 필드 채워짐 | `ok` |
| 캐릭터 미존재 / 기능 미출시 | 200 | `null` 또는 `list[0]` | `empty` — 이탈 아님, "그 시점엔 없었음" |
| 날짜 범위 위반 (미래·당일·API 경계 밖) | 400 | `{"error":{"name":"OPENAPI00004"}}` | `http_400` |
| 레이트리밋 | 429 | `{"error":{"name":"OPENAPI00007"}}` | 재시도 대상 |

→ `empty` 와 `http_400` 은 서로 다른 신호. `empty` 는 데이터로 유지(= 그 시점 캐릭터 부재),
`http_400` 은 잘못된 날짜 요청이므로 재호출/설정 수정 대상.

---

## 3. endpoint별 상세

| endpoint | path | earliest usable | 그 이전 응답 | date 파라미터 | 주의사항 |
|---|---|---|---|---|---|
| `basic` | `/character/basic` | **2023-12-22** | 200 + `character_level=null` | 필수(소급) | 일단위 해상도 있음 |
| `stat` | `/character/stat` | **2023-12-22** | 200 + `final_stat=list[0]` | 필수 | `final_stat` 44개 필드, 전투력은 `stat_name=="전투력"` |
| `hexamatrix` | `/character/hexamatrix` | **2023-12-22** | 200 + `character_hexa_core_equipment=null` | 필수 | 코어 수가 시점별 증가(예: 4→8→10) — 이게 육성 신호 |
| `hexamatrix-stat` | `/character/hexamatrix-stat` | **2025-06-19** | 200 + `character_hexa_stat_core=list[0]` | 필수 | **2025 여름(ASSEMBLE) 이후만.** 그 이전 시즌엔 사용 불가 |
| `symbol` | `/character/symbol-equipment` | **2023-12-22** | 200 + `symbol=list[0]` | 필수 | 심볼 개수·레벨 시점별 증가 관측됨(9→11) |
| `item-equipment` | `/character/item-equipment` | **2023-12-22** | 200 + `item_equipment=list[0]` | 필수 | ⚠️ **프리셋 전환 시 반환 장비 개수가 급변**(24→13 관측). "장비 개수 변화"를 육성 신호로 쓰면 안 됨. 슬롯별 스타포스/잠재로 봐야 함 |
| `union` | `/user/union` | **2023-12-22** | 200 + `union_level=null` | **필수** (생략 시 null 반환) | `union_level` 시점별 변동 = 계정(부캐군) 활동 프록시 |

---

## 4. probe n=5 에서 관찰한 실제 궤적 — **설계 검증용 anecdotal example (일반화 금지)**

5개 캐릭터의 12개월 궤적을 뽑아 "패널로 상태 분류가 가능한가"만 확인한 것이다.
표본이 5명이므로 아래 수치는 비율·분포 근거로 쓰지 않는다.

| 코호트(2026-06 레벨) | 2024-06 | 2025-06 | 2025-12 | 2026-06 | probe 관찰 (예시일 뿐) |
|---|---|---|---|---|---|
| burnend (281) | 270 | 277 | 281 | 281 | 2025 시즌 중 281 도달 후 이후 무변동 |
| at260 (260) | — | 260 | 260 | 260 | 3개 시점 모두 정확히 260, union_level flat |
| past260 (262) | — | 261 | 262 | 262 | 18개월간 +1레벨 |
| climb (256) | — | 220 | 220 | 256 | 2025 내내 220, 2026 시즌에 220→256 |
| approach (251) | — | 208 | 208 | 251 | 2025 내내 208, 2026 시즌에 208→251, 단 union_level 은 증가 |

→ 확인된 것: 캐릭터별 레벨 궤적을 3시즌에 걸쳐 복원 가능하고, "시즌 성장 → 정체 → 다음 시즌
재성장" 형태가 개별 사례에서 나타난다. **상태 분류(Parker/Persistent/Reactivated/Dormant)
규칙은 실제 패널 수집 후 분포를 보고 정의한다 — probe로 정의하지 않는다.**

---

## 5. Part 2 패널 설계에 미치는 제약 요약

1. 2025 시즌 코호트를 **랭킹으로 새로 추출할 수 없다** → 기존 1,958 OCID의 look-back 패널로만 가능.
2. 기존 OCID는 "2026-06-18에 특정 레벨 + 랭킹 진입" 표본 → **2026-06 이전 완전 이탈 계정은 관측
   불가**. 이탈률이 아니라 "살아남은 캐릭터의 상태 분포"만 측정.
3. `hexamatrix-stat` 은 2025 겨울(CROWN) 이전 시점에 못 씀 → 2025 여름 이전 육성 신호에서 제외.
4. `item-equipment` 프리셋 문제로 장비 신호는 슬롯 단위 파싱 필요.
5. CROWN(2025-12-18~2026-06-17)이 6개월 연속이라 2026 상반기엔 "비수기 대조"가 없다.

# 메이플스토리 260 허들 분석 — 구현 스펙문서

작성일: 2026-09-02 · 짝 문서: `메이플스토리_이벤트분석_계획문서.md`

---

## 0. 이 문서의 위치

- **계획문서 = 왜 / 무엇** (질문·가설·코호트 정의·지표 정의·한계). 확정됨.
- **이 문서 = 어떻게** (API 필드, CSV 스키마, 함수 시그니처, 재시도·레이트리밋, 시각화 축, REPORT 목차).
- 계획문서 내용은 복붙하지 않고 `계획문서 §N` 으로 참조한다. 두 문서가 어긋나면 계획문서가 우선.
- **실행 순서**: `collect.py` → `analysis.ipynb` → `REPORT.md`

---

## 1. 환경 · 의존성

- Python 3.10+ (미션 최소 요건 3.10. `X | None` 타입 표기 등 3.10 문법만 사용)
- `requirements.txt` (버전 하한만, 상한 없음):
  ```
  requests>=2.31
  pandas>=2.1
  numpy>=1.26
  scipy>=1.11
  matplotlib>=3.8
  jupyter>=1.0
  ```
- **API 키**: 환경변수 `NXOPEN_API_KEY` 로만 읽는다. 소스·노트북·CSV에 절대 하드코딩 금지.
  - Windows PowerShell: `$env:NXOPEN_API_KEY = "..."`
  - 없으면 `collect.py` 는 즉시 `SystemExit("NXOPEN_API_KEY 환경변수 없음")`.
- 디렉터리 구조 (계획문서 §10 + `collect.py`·`README.md`·`.gitignore` 추가):
  ```
  maplestory-260hurdle-analysis/
  ├── collect.py
  ├── analysis.ipynb
  ├── REPORT.md
  ├── README.md
  ├── requirements.txt
  ├── .gitignore
  ├── data/
  │   ├── cohort_b_event.csv
  │   ├── cohort_b_off.csv
  │   ├── cohort_c1.csv
  │   ├── cohort_c2.csv
  │   ├── breakthrough_retention_summary.csv
  │   └── stagnation_pooled.csv
  └── images/
      ├── 01_breakthrough_off-vs-event.png
      ├── 02_retention_by-level-tier.png
      ├── 03_stagnation_map_200-300.png
      └── 04_growth_trajectory.png
  ```

### 1.1 GitHub 저장소 준비물

- **`README.md`** — 설치(`pip install -r requirements.txt`), 환경변수 설정, 실행 순서
  (`collect.py` → `analysis.ipynb`), 데이터 출처·수집 방법(이 문서 + 계획문서 링크), 라이선스 주의(§2).
  내용은 §1·§3.4·§8 부록의 요약이므로 분량 최소로.
- **`.gitignore`** — `__pycache__/`, `.env`, `.ipynb_checkpoints/`.
- **`data/*.csv` 는 커밋한다** — 넥슨 공개 랭킹 기반 데이터라 재배포 문제 낮고, 재현성(미션 §7)에 필요.
- API 키는 절대 커밋 금지 (환경변수만, §2·§3.1).

---

## 2. Nexon Open API 스펙

Base URL: `https://open.api.nexon.com/maplestory/v1`
공통 헤더: `x-nxopen-api-key: <NXOPEN_API_KEY>`
공통 형식: 응답 JSON, 날짜 파라미터 `date` 는 `YYYY-MM-DD` (KST 기준).

> **라이선스 · 이용약관**: NEXON Open API 이용약관을 준수한다 — 비상업적 포트폴리오 용도,
> 데이터 출처를 "NEXON Open API" 로 표기, 재배포·상업 이용 전 약관 재확인. 출처 링크는 계획문서 §2.
> REPORT.md 데이터 설명 절에도 같은 문구를 넣는다.

| 용도 | 메서드 · 경로 | 파라미터 | 쓰는 응답 필드 |
|---|---|---|---|
| ocid 조회 | `GET /id` | `character_name` | `ocid` (str) |
| 기본정보 | `GET /character/basic` | `ocid`, `date` | `date`, `character_level` (int), `character_exp` (int), `character_exp_rate` (str→float), `character_class` (str), `world_name` (str) |
| 스탯(전투력) | `GET /character/stat` | `ocid`, `date` | `final_stat` (list[{`stat_name`, `stat_value`}]) → `stat_name == "전투력"` 인 항목의 `stat_value` (str→int) |
| 전체 랭킹 | `GET /ranking/overall` | `date`, `world_name`, `world_type`, `page` | `ranking` (list[{`ranking` (int), `character_name` (str), `character_level` (int)}]) |

**미확정 → 구현 시 1건 실호출로 확인할 것** (`# TODO(verify)` 주석 남기기):
- `/character/stat` 응답에서 전투력 키 이름이 정확히 `"전투력"` 인지, 값이 문자열인지.
- `/ranking/overall` 의 `world_type` 필수값 (전체 월드 통합 랭킹이면 `world_type=0` 추정, `world_name` 생략 가능 여부).
- 페이지당 행 수 = 200 (계획문서 §6 의 "랭킹 599,801~600,000 = page 3000" 매핑으로 역산됨. 실호출로 재확인).

### 레이트리밋 · 재시도 규약

- 개발단계 한도: **초당 5회 / 일 1,000회** (계획문서 §7 검증 완료).
- 모든 요청 뒤 `time.sleep(0.25)` 고정 (초당 4회 페이스, 버퍼 포함).
- HTTP `429` / `5xx` → 지수 백오프 `1s → 2s → 4s`, **최대 3회** 재시도. 3회 모두 실패하면 예외로 중단하지 않고
  해당 행 `error_code = "http_429_giveup"` / `"http_5xx_giveup"` 기록 후 다음으로.
- HTTP `404` (계정삭제·개명·랭킹비공개 등) → 재시도 없이 즉시 `error_code = "http_404"`.
- `requests` 타임아웃 `timeout=10`, `Timeout` 예외 → `error_code = "timeout"` (재시도 1회).
- 조회 실패 행은 **"무활동(0)" 이 아니라 별도 이탈 신호** (계획문서 §8). `level`·`combat_power` 는 `<NA>` 로 둔다.
- 소급조회 한계: `date` 는 대략 최근 4개월 이내만 유효 (계획문서 §7 에서 6/18·5/1 검증 완료).

### 2.1 결측치 · 이상치 처리 규약 (미션 §4 "기준 정해 처리")

**결측치 (조회 실패)** — 한곳 정리:

| 상황 | `error_code` | 처리 |
|---|---|---|
| 계정삭제·개명·랭킹비공개 | `http_404` | `level`/`combat_power` `<NA>` |
| 서버 오류 3회 실패 | `http_5xx_giveup` | 〃 |
| 레이트리밋 3회 실패 | `http_429_giveup` | 〃 |
| 타임아웃 재시도 실패 | `timeout` | 〃 |
| 전투력 키 파싱 실패 | `stat_key_missing` | `combat_power` 만 `<NA>` |

→ 결측 행은 **비율 지표 분모에서 제외**하고 `n_dropout` 으로 따로 센다. "무활동(0)" 으로 취급하지 않는다
(계획문서 §8). 리텐션 계산 시 직전/현재 중 하나라도 결측이면 그 구간은 계산 제외.

**이상치** — 판정 및 처리:

| 패턴 | 판정 | 처리 |
|---|---|---|
| `level_N < level_{N-1}` (레벨 감소) | 물리적으로 불가 → 데이터 오류 | 해당 스냅샷 `error_code="anomaly_level_drop"`, 결측과 동일 처리 |
| 전투력이 직전 대비 −30% 초과 하락 | 장비 해제·리롤 등 정상 행위 가능 | **이상치 아님.** 값 유지, `note="cp_drop"` 기록, 활동 신호로는 인정 |
| 레벨 급상승(구간 15레벨+) | 버닝 지정 정황 | **이상치 아님.** §5.3 버닝추정 신호로 사용 |
| 앵커 레벨과 D+1 레벨 괴리 과다 (예: c2 앵커 200 → D+1 280+) | 표본 오추출·개명 재사용 의심 | `note="anchor_mismatch"` 표시 후 개별 검토, 부적합 시 표본에서 제외(제외 인원은 REPORT 한계점에 명시) |

→ 이상치 판정은 `analysis.ipynb` 파생 플래그 셀(§6 셀 4)에서 수행하고, 원본 CSV 는 건드리지 않는다.

---

## 3. `collect.py` 스펙

### 3.1 모듈 상단 상수

```python
API_KEY = os.environ["NXOPEN_API_KEY"]          # 없으면 KeyError → SystemExit 로 감쌈
BASE = "https://open.api.nexon.com/maplestory/v1"
REQ_SLEEP = 0.25
RANDOM_SEED = 42
SAMPLE_N = 22
DATA_DIR = Path(__file__).parent / "data"

# 계획문서 §6 표를 그대로 옮긴 4개 코호트. checkpoints 는 anchor_date + {1,3,7,14,30}일.
COHORTS = [
  dict(name="b_event", anchor_date="2026-06-18", ranking_page=3000,
       rank_lo=599_801, rank_hi=600_000, expected_level=250, roles=("Q1","Q2","Q3")),
  dict(name="b_off",   anchor_date="2026-05-01", ranking_page=3000,
       rank_lo=599_548, rank_hi=599_747, expected_level=248, roles=("Q1","Q3")),
  dict(name="c1",      anchor_date="2026-06-18", ranking_page=300,
       rank_lo=59_801,  rank_hi=60_000,  expected_level=281, roles=("Q2","Q3")),
  dict(name="c2",      anchor_date="2026-06-18", ranking_page=10000,
       rank_lo=1_999_801, rank_hi=2_000_000, expected_level=200, roles=("Q1","Q2","Q3")),
]
CHECKPOINT_OFFSETS = [1, 3, 7, 14, 30]   # D+N
```

### 3.2 함수 시그니처 · 동작

| 함수 | 시그니처 | 동작 |
|---|---|---|
| `_get` | `_get(path: str, params: dict) -> dict \| None` | 헤더 붙여 GET, §2 재시도 규약 적용. 실패 시 `(None, error_code)` 대신 `None` 반환하고 `error_code` 는 호출부에서 판단하도록 예외를 잡아 `("__error__", code)` 튜플로. **구현 단순화**: `_get` 는 `dict` 또는 `{"__error__": code}` 반환. |
| `get_ocid` | `get_ocid(name: str) -> str \| None` | `/id?character_name=`. 실패 시 `None`. |
| `get_basic` | `get_basic(ocid: str, date: str) -> dict` | `/character/basic`. 반환: `{"level","exp","exp_rate"}` 또는 `{"error_code": ...}`. |
| `get_combat_power` | `get_combat_power(ocid: str, date: str) -> int \| None` | `/character/stat` → `final_stat` 에서 전투력 파싱. 실패/키 없음 → `None`. |
| `fetch_ranking_slice` | `fetch_ranking_slice(cohort: dict) -> list[str]` | 앵커일 `ranking_page` 1콜 → `ranking[]` 에서 `rank_lo <= ranking <= rank_hi` 인 `character_name` 리스트 (약 200명). |
| `sample_cohort` | `sample_cohort(names: list[str], cohort_name: str) -> list[str]` | `random.Random(f"{RANDOM_SEED}-{cohort_name}").sample(names, SAMPLE_N)`. 코호트명으로 seed 분기 → 코호트 간 독립·재현 가능. |
| `collect_cohort` | `collect_cohort(cohort: dict) -> pd.DataFrame` | 22명 각각 `get_ocid` 1콜 → 5개 체크포인트마다 `get_basic` + `get_combat_power` 2콜. §4 long-format 행 생성. |
| `main` | `main() -> None` | `COHORTS` 순회. `data/cohort_{name}.csv` 이미 있으면 skip (중단 후 재개용). 코호트별 저장. 마지막에 총 API 호출 수 출력. |

### 3.3 호출 예산 검산 (계획문서 §6)

```
ocid 조회 : 4코호트 × 22명            =  88
스냅샷    : 88명 × 5체크포인트 × 2엔드포인트 = 880
랭킹 슬라이스 : 4콜 (예산 외 소액)
합계 ≈ 968  (개발단계 일 1,000건 대비 32건 버퍼)
```

`main()` 은 실제 호출 카운터를 누적해 968 을 크게 넘으면 경고 로그를 남긴다 (404 재시도 절약분으로 보통 그 이하).

### 3.4 실행

```bash
python collect.py           # data/cohort_*.csv 4개 생성. 하루 예산 안에서 1회 완주.
```

- 중간에 끊기면 다시 실행 → 완성된 CSV 는 건너뛰고 남은 코호트만.
- 하루 한도를 쓰면 다음 날 이어서. (앵커일이 과거 고정이라 날짜 밀려도 데이터 동일)

---

## 4. CSV 스키마

### 4.1 `cohort_{b_event,b_off,c1,c2}.csv` — 수집 원본 (long format)

한 행 = (캐릭터 1명, 체크포인트 1개). 22명 × 5체크포인트 = 코호트당 110행.

| 컬럼 | dtype | 설명 |
|---|---|---|
| `cohort` | str | `"b_event"` / `"b_off"` / `"c1"` / `"c2"` |
| `character_name` | str | 랭킹에서 뽑은 캐릭터명 |
| `ocid` | str | `/id` 결과. 실패 시 빈 문자열 |
| `checkpoint` | str | `"D+1"`, `"D+3"`, `"D+7"`, `"D+14"`, `"D+30"` |
| `date` | str (`YYYY-MM-DD`) | 앵커일 + 오프셋 |
| `level` | Int64 (nullable) | `character_level`. 조회 실패 시 `<NA>` |
| `exp` | Int64 (nullable) | `character_exp` |
| `exp_rate` | float (nullable) | `character_exp_rate` (%) |
| `combat_power` | Int64 (nullable) | 전투력 |
| `error_code` | str (nullable) | `collect.py` 가 채움: `"http_404"` / `"http_429_giveup"` / `"http_5xx_giveup"` / `"timeout"` / `"stat_key_missing"` / `<NA>`(정상) |

- 이상치 플래그(`anomaly_level_drop`, `cp_drop`, `anchor_mismatch`, §2.1)는 **노트북에서 파생 컬럼으로만** 부여 —
  원본 CSV 스키마에는 넣지 않는다.

- D0(앵커일) 스냅샷은 **수집하지 않는다** — 앵커 레벨은 계획문서 §6 `expected_level` 로 고정 취급하고,
  돌파율 D0 기준선은 "전원 미돌파(0)"로 둔다 (b_event/b_off Lv.250·248, c1 Lv.281 은 예외 → §5.1 참고).

### 4.2 `breakthrough_retention_summary.csv` — 분석 산출 (코호트 × 체크포인트 집계)

`analysis.ipynb` 가 생성. 한 행 = (cohort, checkpoint).

| 컬럼 | dtype | 설명 |
|---|---|---|
| `cohort` | str | |
| `checkpoint` | str | `"D+1"`…`"D+30"` |
| `n` | int | 해당 시점 유효 표본 (조회성공 인원) |
| `n_dropout` | int | `error_code` 있는 인원 (이탈 신호) |
| `breakthrough_rate` | float | §5.1 |
| `bt_ci_lo`, `bt_ci_hi` | float | 부트스트랩 95% CI |
| `retention_rate` | float | §5.5 (직전 체크포인트 대비) |
| `ret_ci_lo`, `ret_ci_hi` | float | |
| `settle_rate` | float (nullable) | §5.4. 돌파자 없는 셀은 `<NA>` |

### 4.3 `stagnation_pooled.csv` — 정체구간 지도 (계획문서 §8 "200~300 지도")

한 행 = 캐릭터 1명 (4코호트 전부 풀링, 최대 88행).

| 컬럼 | dtype | 설명 |
|---|---|---|
| `cohort` | str | |
| `character_name` | str | |
| `stagnation_start_level` | Int64 (nullable) | §5.6 규칙으로 산출. 관측 내내 성장 지속 → `<NA>` |
| `note` | str (nullable) | `"burning_suspect"` / `"dropout_before_stagnation"` 등 부가 플래그 |

---

## 5. 지표 계산 스펙 (계획문서 §8 → 컬럼 연산)

모든 계산은 코호트 DF 를 `character_name` 별로 `checkpoint` 순서 정렬 후 수행.
체크포인트 순서 인덱스: `D+1=0, D+3=1, D+7=2, D+14=3, D+30=4`.

### 5.1 260 돌파율 (누적형)

```
brk_raw   = (level >= 260)                       # 조회실패 행은 False
brk_cum   = brk_raw.groupby(character).cummax()  # 한 번 넘으면 이후 계속 1
돌파율(cohort, D+N) = brk_cum[해당 시점].mean()   # 분모 n = 유효표본
```
- c1(앵커 Lv.281)은 전원 이미 260↑ → 돌파율 분석 대상 아님. Q2·Q3 에만 사용.
- D0 기준선 = 0 (앵커 레벨이 250/248/200 이므로 전원 미돌파로 시작).

### 5.2 이벤트 임팩트 — **b_event vs b_off 한정**

```
임팩트(D+N)      = 돌파율_b_event(D+N) - 돌파율_b_off(D+N)        # %p, 절대
상대임팩트(D+N)  = (돌파율_b_event / 돌파율_b_off - 1) * 100      # 분모 0 이면 NaN 표기
```
- c2 는 짝(off 코호트)이 없어 이 계산에서 제외. c2 의 돌파 사례는 §5.4 정착률·§5.3 버닝추정에 보조 증거로만.

### 5.3 버닝추정 플래그 (탐색적)

```
burn_suspect(char) = (D+7 시점까지 level >= 260 도달) OR (관측 중 최대 level - 앵커 level >= 15)
```
- **적용 대상**: `b_event`, `c2` (둘 다 버닝 지정 가능 구간 200~258).
- `b_off`(버닝 이벤트 부재 시점) · `c1`(이미 258↑, 지정 불가) → 전원 `비버닝` 고정.
- 계획문서 §12: 계정당 1캐릭터 제한 → 실제 해당 인원이 극소수일 수 있음. **통계 비교보다 개별 사례** 수준으로 서술.

### 5.4 정착률 (세부질문 1 핵심)

```
settle(char) = 1 if (260 돌파 후 남은 체크포인트 중 최소 1회 level 변화 OR combat_power 변화) else 0
정착률(group) = settle 합 / 그 group 의 260 돌파자 수
```
- group 3갈래: `버닝추정군` / `비버닝군` / `b_off 자연돌파군`(존재 시).
- 돌파가 마지막 체크포인트(D+30)에서 처음 발생 → 남은 관측 없음 → 정착 판정 불가 → `<NA>`, 분모에서 제외.
- 분모가 작으면(특히 c2) 비율 대신 "돌파 k명 중 j명 정착" 원자료로 표기 (계획문서 §12).

### 5.5 D-day 활동 지속률 (리텐션, 구간형)

```
active_N(char) = 1 if (level_N != level_{N-1}) OR (combat_power_N != combat_power_{N-1}) else 0
리텐션(cohort, D+N) = active_N.mean()      # N>=1, 직전 체크포인트 대비 (D0 고정비교 아님)
```
- 조회 실패 행: `active` 계산에서 제외하고 `n_dropout` 로 별도 집계.
- 대상 코호트: `c2`(200) / `b_event`(250) / `c1`(281). 만렙 성장완료군은 계획문서 §3·§4 대로 제외.

### 5.6 정체 시작 레벨 (200~300 지도)

```
연속한 두 체크포인트에서 level 불변 AND combat_power 불변 이 처음 나타나는 구간의
앞쪽 시점 level = stagnation_start_level
```
- 관측 내내 둘 중 하나라도 계속 변함 → `<NA>` (정체 없음).
- 첫 체크포인트부터 조회 실패로 시작 → `note = "dropout_before_stagnation"`, `stagnation_start_level = <NA>`.
- 4코호트 풀링해 `stagnation_pooled.csv` → 히스토그램에서 258~260 / 280~281 집중도 확인 (계획문서 §4 세부질문 3 가설).

### 5.7 신뢰구간 · 유의성 검정

- **부트스트랩 95% CI**: 비율 지표(돌파율·리텐션·정착률)에 적용. `n_resamples = 10_000`,
  `rng = np.random.default_rng(42)`, 표본에서 복원추출 → 각 재표집의 비율 → 2.5/97.5 백분위.
  - `scipy.stats.bootstrap` 사용 가능하면 그것으로 (`method="percentile"`). 표본 22명이라 CI 가 넓게 나오는 것 자체가 표본 한계의 정직한 표시 (계획문서 §8).
- **Fisher's exact test** (`scipy.stats.fisher_exact`): 그룹 간 돌파율 차이 2×2 검정.
  - 적용 짝: `b_event vs b_off` 의 D+30 돌파/미돌파. (표본 작아 카이제곱보다 적합, 계획문서 §8.)
- **이동평균**: 궤적 그래프 노이즈 완화용 (체크포인트 5개뿐이라 window=2, 보조용).

### 5.8 레벨 증가율 (변화율)

```
elapsed_day = {D+1:1, D+3:3, D+7:7, D+14:14, D+30:30}
growth_rate_N(char) = (level_N - level_{N-1}) / (elapsed_day_N - elapsed_day_{N-1})   # 레벨/일
```
- 구간별 성장 속도. 결측 구간은 `<NA>`.
- 용도: (1) §5.3 버닝추정의 정량 근거, (2) 세부질문 2 — 어느 구간에서 성장 속도가 꺾이는지,
  (3) `04_growth_trajectory` 기울기 해석.
- 코호트별 구간 평균 `growth_rate` 를 D+3/7/14/30 구간에 대해 표로 REPORT 에 제시.

**적용하는 시계열 기법 (미션 §4 "최소 2가지")**: ① 이동평균 ② 구간별 통계 비교(§5.2) ③ 변화율(§5.8)
④ 부트스트랩 재표집 CI(§5.7) — 총 4종.

---

## 6. `analysis.ipynb` 셀 구성

| # | 셀 | 내용 |
|---|---|---|
| 1 | import · 로드 | `pandas/numpy/scipy/matplotlib`. `data/cohort_*.csv` 4개 로드, §4.1 dtype 명시 지정 (`dtype={...}`, `parse_dates=["date"]`). |
| 2 | **데이터 기본정보 확인** (미션 §4) | `date` min/max(기간), 컬럼·dtype 목록, 코호트별 행수(기대 110), `df.isna().sum()` 결측치 표, `checkpoint` 값 분포, `character_name`×`checkpoint` 중복·누락 점검, 날짜 정렬 확인. |
| 3 | 클린 · 이탈 집계 | `error_code` 있는 행 분리 → 코호트별 `n_dropout` 표. 정상 행만 분석 DF 로. §2.1 결측 규약 적용. |
| 3.5 | **시계열 특성** (markdown, 미션 §3) | 이 데이터의 **트렌드** = 돌파율·전투력의 우상향 경향 / **계절성** = 해당 없음(D+1·3·7·14·30 비정기 5점, 주기 구조 부재 — 근거 명시) / **노이즈** = 개별 궤적의 소폭 진동 → 이동평균(window=2)으로 완화. |
| 4 | 파생 플래그 · 이상치 판정 | §5.1 `brk_cum`, §5.3 `burn_suspect`, §5.5 `active_N`, §5.6 `stagnation_start_level`, §5.8 `growth_rate` 컬럼 추가. **§2.1 이상치 규칙** 적용(`anomaly_level_drop` 부여, `note` 기록, `anchor_mismatch` 개별 검토). |
| 5 | Q1 돌파·정착 | §5.1 돌파율(b_event/b_off/c2), §5.2 임팩트, §5.4 정착률(3갈래), §5.7 Fisher + 부트스트랩. → `breakthrough_retention_summary.csv` 기록. |
| 6 | Q2 레벨대별 리텐션 | §5.5 리텐션 곡선 c2/b_event/c1 + §5.8 구간 성장률 표. CI 포함. |
| 7 | Q3 정체 지도 | §5.6 풀링 → `stagnation_pooled.csv` 기록. 258~260 / 280~281 집중도 수치. |
| 8 | 시각화 4종 | §7 스펙대로 `images/*.png` 저장. |
| 9 | 인사이트 (markdown 셀) | 3개 이상. 각 인사이트를 **관찰(Fact — 그래프에서 읽은 수치·구간)** / **원인(Why — 가설 1~2, 반례 가능)** / **행동(Action — 다음 분석 또는 계획문서 §9 실무 제언 연결)** 3단으로 (미션 §8, 계획문서 §1). |

---

## 7. 시각화 4종 스펙

공통: `matplotlib`, `figsize=(9,5)`, `dpi=150`, 한글 폰트 지정(`Malgun Gothic`), 제목·축라벨 한국어, 범례 우상단.

**시각화 가이드 (미션 §8):**
- **축**: x축은 체크포인트를 균등 배치하지 않고 **실제 경과일(1/3/7/14/30) 척도**로 그린다 — 비정기 간격 왜곡 방지.
  날짜 정렬·누락 구간 점검은 §6 셀 2에서 선행.
- **스케일**: 돌파율·리텐션 y축은 `0~1` 고정. 전투력 등 값 범위 큰 지표는 로그 스케일 검토 후 캡션에 명시.
- **집계 단위**: 각 그래프 캡션에 "집계 단위 = D-day 경과일, 이벤트 초반 변화를 포착하려 비균등 체크포인트 사용" 1줄 근거를 넣는다.

| 파일 | 차트 | x축 | y축 | 시리즈 | CI / 강조 |
|---|---|---|---|---|---|
| `01_breakthrough_off-vs-event.png` | 선그래프 | 경과일(1·3·7·14·30) | 260 돌파율 (0~1 고정) | `b_off`, `b_event` | 시리즈별 부트스트랩 CI 를 `fill_between` 음영 |
| `02_retention_by-level-tier.png` | 선그래프 | 경과일 | 리텐션율 (0~1 고정) | `c2`(Lv.200) / `b_event`(Lv.250) / `c1`(Lv.281) | CI 음영. 250 시리즈 강조색 |
| `03_stagnation_map_200-300.png` | 히스토그램 | 정체 시작 레벨 (bin=1, 200~300) | 캐릭터 수 | 풀링 1계열 (코호트별 색 스택 optional) | `axvspan` 으로 258–260·280–281 강조 |
| `04_growth_trajectory.png` | 스파게티(선) | 경과일 | 레벨 | 캐릭터별 얇은 선 (b_event + c2) | `burn_suspect` 인 캐릭터만 진한 강조색, 나머지 회색. y=260 수평선 |

---

## 8. `REPORT.md` 목차

1. **분석 주제 및 선정 이유** — 계획문서 §1·§2 요약.
2. **분석 질문 (3개 이상)** — 계획문서 §4 핵심 질문 + 세부질문 1–3.
3. **데이터 설명** — 출처(NEXON Open API), 기간, 4코호트 표(계획문서 §6), 데이터 수(88명×5=440행).
   **라이선스 주의 문구**(§2). **시계열 특성** 문단(트렌드/계절성 해당 없음/노이즈, §6 셀 3.5 요약).
4. **데이터 정제** — 결측치·이상치 처리 기준(§2.1 표 요약), 실제 제외/보정 건수, `n_dropout` 코호트별 수치.
5. **분석 기법과 적용 이유** — 이동평균·구간별 통계·변화율(§5.8)·부트스트랩 CI 를 왜/어떻게 썼는지 각 1~2줄.
6. **결과 Q1 · 돌파와 정착** — 돌파율/임팩트/정착률. 각 소절 **관찰(수치)** → **해석(가설)** 분리.
7. **결과 Q2 · 레벨대별 리텐션 + 구간 성장률**
8. **결과 Q3 · 정체 시작 지점 지도**
9. **인사이트 (3개 이상)** — 각 인사이트 = 관찰(Fact) / 원인(Why) / 행동(Action) 3단 (미션 §8).
10. **결론** — 핵심 질문("260까지 키우면 계속 키우나 방치하나 / 이벤트가 이를 어떻게 바꾸나")에 대한 답 3~5줄 요약.
11. **한계점** — 계획문서 §12 전체 + 시리즈당 5점이라는 표본 구조 한계 한 줄.
12. **AI 사용 로그** — 계획문서 §11 3항목(사용 작업 / 사용 이유 / 검증 방법)을 단계별로.
13. **부록 · 재현 방법** — 환경변수 설정, `python collect.py`, 노트북 실행 순서 (README 와 동일 내용).

---

## 9. 미션 요구사항 ↔ 스펙 매핑

| 미션 요구사항 | 충족 위치 |
|---|---|
| 시계열 데이터 1개, 100+ 포인트 | §4.1 — 88명 × 5체크포인트 = 440행 (계획문서 §6) |
| 출처·기간·라이선스 명시 | §2 라이선스 문구, §8 REPORT 3절, §1.1 README |
| 분석 질문 3개 이상 | §8 REPORT 2절 (계획문서 §4 세부질문 1–3) |
| 데이터 기본정보(기간·컬럼·결측) 확인 | §6 셀 2 |
| 결측치/이상치 기준 정해 처리 | §2.1 규약, §6 셀 3·4, §8 REPORT 4절 |
| 시계열 트렌드/계절성/노이즈 개념 설명 | §6 셀 3.5, §8 REPORT 3절 |
| 시계열 분석 기법 2가지 이상 | §5.8 말미 — 이동평균·구간통계·변화율·부트스트랩 4종. 왜/어떻게는 §8 REPORT 5절 |
| 시각화 2개 이상(3개+ 권장) | §7 (4종), 축·스케일·집계단위 가이드 포함 |
| 인사이트 3개 이상, 관찰 근거 포함 | §6 셀 9 — 관찰(Fact)/원인(Why)/행동(Action) 3단, §8 REPORT 9절 |
| 관찰(근거) / 해석(가설) 구분 서술 | §6 셀 9, §8 REPORT 6–8절 소제목 규칙 |
| REPORT: 주제·질문·데이터·시각화·인사이트·결론·한계 | §8 REPORT 1·2·3·6–8·9·10·11절 |
| requirements.txt / 실행 방법 | §1, §1.1 README, §8 REPORT 13절 |
| GitHub 저장소 (코드·리포트·데이터/문서) | §1 구조 + §1.1 (README·.gitignore, data 커밋) |
| AI 사용 로그 3항목 | §8 REPORT 12절 (계획문서 §11) |
| Python 3.10 이상 | §1 |
| (보너스) 대시보드 / 시계열 분해·예측 | §10 — 미수행 사유 명시, 대시보드는 계획문서 §13 로드맵 |

---

## 10. 구현 체크리스트

- [ ] `collect.py` — §3 함수 8개. 첫 실행 전 §2 "미확정" 3건을 1건 실호출로 확인하고 `# TODO(verify)` 해소.
- [ ] `python collect.py` 완주 → `data/cohort_*.csv` 4개 (각 110행, `error_code` 컬럼 포함).
- [ ] `analysis.ipynb` — §6 셀 1~9(중 3.5 markdown). `breakthrough_retention_summary.csv`, `stagnation_pooled.csv` 생성.
- [ ] `images/*.png` 4개 — §7 (축·스케일·집계단위 가이드 반영).
- [ ] `REPORT.md` — §8 13절 목차. 결측/이상치·시계열 특성·인사이트 3단·결론·라이선스·한계·AI 로그 포함.
- [ ] `requirements.txt` — §1.
- [ ] `README.md` · `.gitignore` — §1.1.

### 보너스 (미션 §5) — 이번 범위 밖

- 시계열 분해/예측: 코호트당 체크포인트 5개(비정기)라 STL 분해·베이스라인 예측 모두 부적합 → **미수행**.
- 대시보드: 계획문서 §13 로드맵(9/16 이벤트 종료 후 Streamlit)으로 이관.

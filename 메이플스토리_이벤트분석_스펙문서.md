# 메이플스토리 260 허들 분석 — 구현 스펙문서

작성일: 2026-09-02 · 짝 문서: `메이플스토리_이벤트분석_계획문서.md`

---

## 0. 이 문서의 위치

- **계획문서 = 왜 / 무엇** (질문·가설·코호트 정의·지표 정의·한계). 확정됨.
- **이 문서 = 어떻게** (API 필드, CSV 스키마, 함수 시그니처, 재시도·레이트리밋, 시각화 축, REPORT 목차).
- 계획문서 내용은 복붙하지 않고 `계획문서 §N` 으로 참조한다. 두 문서가 어긋나면 계획문서가 우선.
- **실행 순서**: `collect.py` → `analysis.ipynb` → `REPORT.md`

---

## 1. 환경 · 의존성

- Python 3.10+ (미션 최소 요건 3.10. `X | None` 타입 표기 등 3.10 문법만 사용)
- `requirements.txt` (버전 하한만, 상한 없음):
  ```
  requests>=2.31
  pandas>=2.1
  numpy>=1.26
  scipy>=1.11
  matplotlib>=3.8
  jupyter>=1.0
  ```
- **API 키**: 환경변수 `NXOPEN_API_KEY` 로만 읽는다. 소스·노트북·CSV에 절대 하드코딩 금지.
  - Windows PowerShell: `$env:NXOPEN_API_KEY = "..."`
  - 없으면 `collect.py` 는 즉시 `SystemExit("NXOPEN_API_KEY 환경변수 없음")`.
- 디렉터리 구조 (계획문서 §10 + `collect.py`·`README.md`·`.gitignore` 추가):
  ```
  maplestory-260hurdle-analysis/
  ├── collect.py
  ├── analysis.ipynb
  ├── REPORT.md
  ├── README.md
  ├── requirements.txt
  ├── .gitignore
  ├── data/
  │   ├── cohort_approach.csv
  │   ├── cohort_at260.csv
  │   ├── cohort_past260.csv
  │   ├── cohort_burnend.csv
  │   ├── breakthrough_retention_summary.csv
  │   └── stagnation_pooled.csv
  └── images/
      ├── 01_breakthrough_curve.png
      ├── 02_retention_by-level-tier.png
      ├── 03_stagnation_map.png
      └── 04_growth_trajectory.png
  ```

### 1.1 GitHub 저장소 준비물

- **`README.md`** — 설치(`pip install -r requirements.txt`), 환경변수 설정, 실행 순서
  (`collect.py` → `analysis.ipynb`), 데이터 출처·수집 방법(이 문서 + 계획문서 링크), 라이선스 주의(§2).
  내용은 §1·§3.4·§8 부록의 요약이므로 분량 최소로.
- **`.gitignore`** — `__pycache__/`, `.env`, `.ipynb_checkpoints/`.
- **`data/*.csv` 는 커밋한다** — 넥슨 공개 랭킹 기반 데이터라 재배포 문제 낮고, 재현성(미션 §7)에 필요.
- API 키는 절대 커밋 금지 (환경변수만, §2·§3.1).

---

## 2. Nexon Open API 스펙

Base URL: `https://open.api.nexon.com/maplestory/v1`
공통 헤더: `x-nxopen-api-key: <NXOPEN_API_KEY>`
공통 형식: 응답 JSON, 날짜 파라미터 `date` 는 `YYYY-MM-DD` (KST 기준).

> **라이선스 · 이용약관**: NEXON Open API 이용약관을 준수한다 — 비상업적 포트폴리오 용도,
> 데이터 출처를 "NEXON Open API" 로 표기, 재배포·상업 이용 전 약관 재확인. 출처 링크는 계획문서 §2.
> REPORT.md 데이터 설명 절에도 같은 문구를 넣는다.

| 용도 | 메서드 · 경로 | 파라미터 | 쓰는 응답 필드 |
|---|---|---|---|
| ocid 조회 | `GET /id` | `character_name` | `ocid` (str) |
| 기본정보 | `GET /character/basic` | `ocid`, `date` | `date`, `character_level` (int), `character_exp` (int), `character_exp_rate` (str→float), `character_class` (str), `world_name` (str) |
| 스탯(전투력) | `GET /character/stat` | `ocid`, `date` | `final_stat` (list[{`stat_name`, `stat_value`}]) → `stat_name == "전투력"` 인 항목의 `stat_value` (str→int) |
| 전체 랭킹 | `GET /ranking/overall` | `date`, `world_name`, `world_type`, `page` | `ranking` (list[{`ranking` (int), `character_name` (str), `character_level` (int)}]) |

**미확정 → 구현 시 1건 실호출로 확인할 것** (`# TODO(verify)` 주석 남기기):
- `/character/stat` 응답에서 전투력 키 이름이 정확히 `"전투력"` 인지, 값이 문자열인지.
- `/ranking/overall` 의 `world_type` 필수값 (전체 월드 통합 랭킹이면 `world_type=0` 추정, `world_name` 생략 가능 여부).
- 페이지당 행 수 = 200 (계획문서 §6 의 "랭킹 599,801~600,000 = page 3000" 매핑으로 역산됨. 실호출로 재확인).

### 레이트리밋 · 재시도 규약

- 개발단계 한도: **초당 5회 / 일 1,000회** (계획문서 §7 검증 완료).
- 모든 요청 뒤 `time.sleep(0.25)` 고정 (초당 4회 페이스, 버퍼 포함).
- HTTP `429` / `5xx` → 지수 백오프 `1s → 2s → 4s`, **최대 3회** 재시도. 3회 모두 실패하면 예외로 중단하지 않고
  해당 행 `error_code = "http_429_giveup"` / `"http_5xx_giveup"` 기록 후 다음으로.
- HTTP `404` (계정삭제·개명·랭킹비공개 등) → 재시도 없이 즉시 `error_code = "http_404"`.
- `requests` 타임아웃 `timeout=10`, `Timeout` 예외 → `error_code = "timeout"` (재시도 1회).
- 조회 실패 행은 **"무활동(0)" 이 아니라 별도 이탈 신호** (계획문서 §8). `level`·`combat_power` 는 `<NA>` 로 둔다.
- 소급조회 한계: `date` 는 대략 최근 4개월 이내만 유효 (계획문서 §7 에서 6/18·5/1 검증 완료).

### 2.1 결측치 · 이상치 처리 규약 (미션 §4 "기준 정해 처리")

**결측치 (조회 실패)** — 한곳 정리:

| 상황 | `error_code` | 처리 |
|---|---|---|
| 계정삭제·개명·랭킹비공개 | `http_404` | `level`/`combat_power` `<NA>` |
| 서버 오류 3회 실패 | `http_5xx_giveup` | 〃 |
| 레이트리밋 3회 실패 | `http_429_giveup` | 〃 |
| 타임아웃 재시도 실패 | `timeout` | 〃 |
| 전투력 키 파싱 실패 | `stat_key_missing` | `combat_power` 만 `<NA>` |

→ 결측 행은 **비율 지표 분모에서 제외**하고 `n_dropout` 으로 따로 센다. "무활동(0)" 으로 취급하지 않는다
(계획문서 §8). 리텐션 계산 시 직전/현재 중 하나라도 결측이면 그 구간은 계산 제외.

**이상치** — 판정 및 처리:

| 패턴 | 판정 | 처리 |
|---|---|---|
| `level_N < level_{N-1}` (레벨 감소) | 물리적으로 불가 → 데이터 오류 | 해당 스냅샷 `error_code="anomaly_level_drop"`, 결측과 동일 처리 |
| 전투력이 직전 대비 −30% 초과 하락 | 장비 해제·리롤 등 정상 행위 가능 | **이상치 아님.** 값 유지, `note="cp_drop"` 기록, 활동 신호로는 인정 |
| 레벨 급상승(구간 15레벨+) | 버닝 지정 정황 | **이상치 아님.** §5.3 버닝추정 신호로 사용 |
| 앵커(D+0) 레벨이 코호트 목표대와 크게 다름 (예: `approach` 인데 D+0 에 275+) | 표본 오추출·개명 재사용 의심 | `note="anchor_mismatch"` 표시 후 개별 검토, 부적합 시 표본 제외(제외 인원 REPORT 한계점에 명시) |

→ 이상치 판정은 `analysis.ipynb` 파생 플래그 셀(§6 셀 4)에서 수행하고, 원본 CSV 는 건드리지 않는다.

---

## 3. `collect.py` 스펙

> 코호트·시간축은 계획문서 "설계 변경 2"(랭킹 실측 후 재정의)를 따른다.
> 랭킹 page↔레벨, 전투력 키, `world_type=0`, page 당 200행 은 실호출로 확인 완료.

### 3.1 모듈 상단 상수

```python
BASE = "https://open.api.nexon.com/maplestory/v1"
REQ_SLEEP = 0.25
TIMEOUT = 10
MAX_RETRY = 3;  BACKOFF = [1, 2, 4]
RANDOM_SEED = 42
DAILY_CALL_BUDGET = 1000        # 개발단계 일 한도
DATA_DIR = Path(__file__).parent / "data"

RANKING_WORLD_TYPE = 0          # 0=일반 서버 (실측 확인)
ANCHOR = "2026-06-18"          # 여름 성장 이벤트 시작 = 시계열 앵커
DAILY_DAYS = 25               # D+0 .. D+24 매일 basic
STAT_OFFSETS = [0, 8, 16, 24]  # 전투력은 이 오프셋에만 (basic 은 매일)
SAMPLE_N = 100               # 코호트당 표본

# 실측 랭킹 page↔레벨 (2026-06-18, world_type=0). page N = 랭킹 (N-1)*200+1 .. N*200
COHORTS = [
  dict(name="approach", page=21000, target_level=251, desc="허들 접근 (260 미만)"),
  dict(name="at260",    page=15000, target_level=260, desc="260 정체 플래토 (rank ~3M)"),
  dict(name="past260",  page=10000, target_level=262, desc="260 직후"),
  dict(name="burnend",  page=2500,  target_level=281, desc="버닝 BEYOND 종료 지점"),
]
```

### 3.2 함수 시그니처 · 동작

| 함수 | 시그니처 | 동작 |
|---|---|---|
| `_get` | `_get(path, params) -> dict` | 헤더 붙여 GET + §2 재시도 규약. 성공 시 응답 JSON, 실패 시 `{"__error__": code}`. |
| `_load_dotenv` / `_api_key` | `-> None` / `-> str` | `.env` 를 `os.environ` 에 로드(이미 있으면 유지). 키 없으면 `sys.exit`. |
| `daily_dates` | `daily_dates(anchor, n) -> list[str]` | `["2026-06-18", …, "2026-07-12"]` (25일). |
| `stat_dates` | `stat_dates(anchor, offsets) -> list[str]` | 전투력 수집일. |
| `page_rank_range` | `page_rank_range(page) -> (lo, hi)` | `((page-1)*200+1, page*200)`. |
| `parse_combat_power` | `parse_combat_power(final_stat) -> int \| None` | `final_stat` 에서 `stat_name=="전투력"` 값. |
| `sample_cohort` | `sample_cohort(names, cohort_name, n=None) -> list[str]` | `random.Random(f"{RANDOM_SEED}-{cohort_name}").sample(names, SAMPLE_N)`. 코호트명 seed 분기 → 코호트 간 독립·재현. |
| `get_ocid` | `get_ocid(name) -> str \| None` | `/id`. 결과를 `_ocid_cache` 에 저장(25일 재사용). |
| `get_basic` | `get_basic(ocid, date) -> dict` | `{"level","exp","exp_rate"}` 또는 `{"error_code": …}`. |
| `get_combat_power` | `get_combat_power(ocid, date) -> (int\|None, str\|None)` | `(전투력, None)` / `(None, code)`. 키 없으면 `code="stat_key_missing"`. |
| `fetch_ranking_slice` | `fetch_ranking_slice(cohort) -> list[str]` | 앵커일 `page` 1콜 → `page_rank_range` 구간의 `character_name` (~200명). 슬라이스 레벨 로그 출력. |
| `collect_cohort` | `collect_cohort(cohort) -> None` | 표본 100명 × 25일. 각 캐릭터: `get_ocid` 1콜 → 매일 `get_basic`, `STAT_OFFSETS` 날짜만 `get_combat_power` 추가. **캐릭터 1명 끝날 때마다 `data/cohort_{name}.csv` 에 append.** |
| `main` | `main() -> None` | `COHORTS` 순회. 일 한도 도달 또는 일시적 실패 감지 시 중단. |

**재개**: `_load_done(path)` 가 CSV 의 `(character_name, date)` 집합을 읽어 이미 있는 조합은 건너뛴다.
일시적 실패(`http_429_giveup`/`http_5xx_giveup`/`timeout`) 행은 **CSV 에 쓰지 않는다** → 다음 실행에서 자동 재시도.
`http_404`·`ocid_failed`·`stat_key_missing` 은 최종 상태로 기록(재시도 안 함).

### 3.3 호출 예산

```
ocid       : 4코호트 × 100명                    =    400
basic(일간): 4 × 100 × 25일                     = 10,000
stat       : 4 × 100 × 4점                      =  1,600
랭킹 슬라이스: 4콜
합계 ≈ 12,000  →  개발단계 일 1,000건 한도로 약 12일 나눠 수집
```

### 3.4 실행

```bash
python collect.py     # data/cohort_{approach,at260,past260,burnend}.csv 생성/이어쓰기
```

- 일 한도(1,000)에 걸리거나 일시적 실패가 나면 그 지점에서 멈춘다.
- **다음 날 같은 명령 재실행** → 이미 수집된 `(캐릭터, 날짜)` 는 건너뛰고 이어서.
- 앵커일이 과거 고정이라 실행일이 며칠 밀려도 데이터 동일.

---

## 4. CSV 스키마

### 4.1 `cohort_{approach,at260,past260,burnend}.csv` — 수집 원본 (long format)

한 행 = (캐릭터 1명, 날짜 1일). 100명 × 25일 = 코호트당 최대 2,500행.

| 컬럼 | dtype | 설명 |
|---|---|---|
| `cohort` | str | `"approach"` / `"at260"` / `"past260"` / `"burnend"` |
| `character_name` | str | 앵커일 랭킹 슬라이스에서 뽑은 캐릭터명 |
| `ocid` | str | `/id` 결과. 실패 시 빈 문자열 |
| `date` | str (`YYYY-MM-DD`) | 앵커일(포함) ~ +24일 중 하루 |
| `level` | Int64 (nullable) | `character_level`. 조회 실패 시 `<NA>` |
| `exp` | Int64 (nullable) | `character_exp` |
| `exp_rate` | float (nullable) | `character_exp_rate` (%) |
| `combat_power` | Int64 (nullable) | 전투력. **`STAT_OFFSETS` 날짜(D+0/8/16/24)만 값 있음**, 나머지 날 `<NA>` (정상) |
| `error_code` | str (nullable) | `collect.py`: `"http_404"` / `"ocid_failed"` / `"stat_key_missing"` / `<NA>`(정상). 일시적 실패 행은 저장 안 함(§3.2) |

- 앵커일(D+0) 스냅샷을 **직접 수집**하므로 캐릭터별 실제 시작 레벨이 CSV 에 있다 (계획문서 옛 `expected_level` 가정 폐기).
- 이상치 플래그(`anomaly_level_drop`, `cp_drop`, `anchor_mismatch`, §2.1)는 **노트북 파생 컬럼**으로만 부여.

### 4.2 `breakthrough_retention_summary.csv` — 분석 산출 (코호트 × 날짜 집계)

`analysis.ipynb` 가 생성. 한 행 = (cohort, date).

| 컬럼 | dtype | 설명 |
|---|---|---|
| `cohort` | str | |
| `date` | str | 앵커일 ~ +24일 |
| `elapsed_day` | int | 0 ~ 24 (앵커 기준 경과일) |
| `n` | int | 해당 날짜 유효 표본 (조회성공 인원) |
| `n_dropout` | int | `error_code` 있는 인원 (이탈 신호) |
| `breakthrough_rate` | float | §5.1 (approach·at260 대상) |
| `bt_ci_lo`, `bt_ci_hi` | float | 부트스트랩 95% CI |
| `retention_rate` | float | §5.5 (전일 대비) |
| `ret_ci_lo`, `ret_ci_hi` | float | |
| `settle_rate` | float (nullable) | §5.4. 돌파자 없는 셀은 `<NA>` |

### 4.3 `stagnation_pooled.csv` — 정체 시작 레벨 지도 (계획문서 §8 세부질문 3)

한 행 = 캐릭터 1명 (4코호트 풀링, 최대 400행).

| 컬럼 | dtype | 설명 |
|---|---|---|
| `cohort` | str | |
| `character_name` | str | |
| `anchor_level` | Int64 | D+0 관측 레벨 |
| `stagnation_start_level` | Int64 (nullable) | §5.6 규칙. 관측 내내 성장 지속 → `<NA>` |
| `note` | str (nullable) | `"burning_suspect"` / `"dropout_before_stagnation"` 등 |

---

## 5. 지표 계산 스펙 (계획문서 §8 → 컬럼 연산)

모든 계산은 코호트 DF 를 `character_name` 별로 `date` 오름차순 정렬 후 수행.
`elapsed_day` = `(date − ANCHOR).days` (0~24). "전일" = `elapsed_day` 1 작은 행.

### 5.1 260 돌파율 (누적형)

```
brk_raw = (level >= 260)                          # 조회실패 행은 False
brk_cum = brk_raw.groupby(character_name).cummax()  # 한 번 넘으면 이후 계속 1
돌파율(cohort, elapsed_day) = brk_cum[해당 날짜].mean()   # 분모 n = 그날 유효표본
```
- **대상**: `approach`(D+0 Lv.251) — 25일 안에 260 도달률. `at260`(D+0 Lv.260) — 정확히는 "261 이탈률"
  = `(level >= 261).cummax().mean()` 로 따로 계산(260 플래토 탈출).
- `past260`(이미 262)·`burnend`(이미 281)는 돌파율 대상 아님 — 정착·정체(§5.4·§5.6)에만.
- D+0 돌파율은 실제 관측 레벨로 계산(앵커 스냅샷을 직접 수집하므로 가정값 불필요).

### 5.2 이벤트 임팩트 (보조 — 여력 시)

계획문서 설계변경 2: 5/1 과 6/18 랭킹 분포가 거의 같아 이 비교의 기대 효과는 작음.
보조 수집(같은 4 page slice @ `2026-05-01`, n=40, D+0/7/14/24 basic)을 하면:

```
임팩트(day)     = 260돌파율_6/18(day) − 260돌파율_5/1(day)        # %p
상대임팩트(day) = (돌파율_6/18 / 돌파율_5/1 − 1) × 100          # 분모 0 → NaN
```
보조 수집을 못 하면 이 절은 생략하고 한계점에 명시.

### 5.3 버닝추정 플래그 (탐색적)

```
burn_suspect(char) = (D+7 이내 level >= 260 도달) OR (관측 중 최대 level − D+0 level >= 15)
```
- **적용 대상**: `approach`, `at260` (버닝 지정 가능 구간에서 출발 가능). `past260`·`burnend` 는 전원 `비버닝` 고정.
- 계정당 1캐릭터 제한 → 해당 인원 극소수 가능. **통계 비교보다 개별 사례** 수준으로 서술(계획문서 §12).

### 5.4 정착률 (세부질문 1 핵심)

```
settle(char) = 1 if (260 돌파 후 남은 날짜 중 최소 1회 level 변화 OR combat_power 변화) else 0
정착률(group) = Σ settle / 그 group 의 260 돌파자 수
```
- group: `버닝추정군` / `비버닝군` (approach·at260 돌파자 기준).
- 돌파가 마지막 날(D+24)에 처음 발생 → 남은 관측 없음 → `<NA>`, 분모 제외.
- combat_power 는 4일만 있으므로 **정착 판정의 1차 신호는 level 변화**, combat_power 변화는 보강.
- 분모가 작으면 비율 대신 "돌파 k명 중 j명 정착" 원자료로 표기 (계획문서 §12).

### 5.5 활동 지속률 (리텐션, 구간형)

```
active_t(char) = 1 if (level_t != level_{t-1}) OR (전투력 관측일이고 combat_power_t != 직전관측 combat_power) else 0
리텐션(cohort, elapsed_day) = active_t.mean()      # 전일 대비 (D+0 고정비교 아님)
```
- 전일이 결측이면 그 구간은 계산 제외. `n_dropout` 별도 집계.
- **대상 4코호트 전부.** 레벨대별(approach/at260/past260/burnend) 리텐션 곡선을 겹쳐 비교 (세부질문 2).

### 5.6 정체 시작 레벨 (세부질문 3 지도)

```
연속한 며칠 동안 level 불변이 처음 시작되는 그 레벨 = stagnation_start_level
  (정의: level_t == level_{t-1} 이 STALL_DAYS=3 일 이상 이어지는 첫 지점의 level)
```
- 관측 내내 계속 레벨업 → `<NA>` (정체 없음).
- 첫날부터 조회 실패 → `note="dropout_before_stagnation"`, `stagnation_start_level=<NA>`.
- 4코호트 풀링 → `stagnation_pooled.csv` → 히스토그램에서 **258~260 및 280~281 집중도** 확인.
  `at260` 플래토(전원 260 출발)는 "260에서 시작해 며칠째 그대로면 260 정체"로 자연 집계됨.

### 5.7 신뢰구간 · 유의성 검정

- **부트스트랩 95% CI**: 비율 지표(돌파율·리텐션·정착률)에 적용. `scipy.stats.bootstrap`
  (`method="percentile"`, `n_resamples=10_000`, `rng=np.random.default_rng(42)`).
- **Fisher's exact test** (`scipy.stats.fisher_exact`): 2×2 돌파율 차이 검정.
  - 짝: `approach` 의 버닝추정군 vs 비버닝군 (D+24 260 돌파/미돌파). 보조 수집 시 6/18 vs 5/1 도.
- **이동평균**: 코호트 일간 곡선 노이즈 완화 (`window=3`, 중심).

### 5.8 레벨 증가율 (변화율)

```
growth_rate_t(char) = level_t − level_{t-1}       # 레벨/일 (일간이라 분모 1)
코호트 일간 평균 = growth_rate_t.mean()
```
- 결측 구간은 `<NA>`. 7일 이동평균으로 평활한 곡선도 함께.
- 용도: 성장 속도가 꺾이는 날/레벨 파악(세부질문 2·3), 버닝추정 보강, `04_growth_trajectory` 기울기.

**적용 시계열 기법 (미션 §4 "최소 2가지")**: ① 이동평균(§5.7·§5.8) ② 변화율(§5.8)
③ 구간별 통계 비교(§5.2·§5.5 코호트 간) ④ 부트스트랩 재표집 CI(§5.7) — 총 4종.

---

## 6. `analysis.ipynb` 셀 구성

| # | 셀 | 내용 |
|---|---|---|
| 1 | import · 로드 | `pandas/numpy/scipy/matplotlib`. `data/cohort_*.csv` 4개 로드·concat, §4.1 dtype 지정 (`parse_dates=["date"]`). `elapsed_day` 파생. |
| 2 | **데이터 기본정보 확인** (미션 §4) | `date` min/max(기간), 컬럼·dtype, 코호트별 행수(기대 ≤2,500), `df.isna().sum()` 결측 표, 캐릭터 수, `character_name`×`date` 중복·누락(빠진 날) 점검, 날짜 연속성 확인. |
| 3 | 클린 · 이탈 집계 | `error_code` 있는 행 분리 → 코호트별·날짜별 `n_dropout`. 정상 행만 분석 DF 로. §2.1 결측 규약. |
| 3.5 | **시계열 특성** (markdown, 미션 §3) | **트렌드** = 코호트 평균 레벨/돌파율의 우상향 / **계절성** = 해당 없음(관측 25일, 주간 주기 볼 여지 적음 — 요일별 평균으로 확인만 하고 근거와 함께 기각) / **노이즈** = 개별 궤적 진동·조회 실패 → 이동평균(window=3)·CI 로 대응. |
| 4 | 파생 플래그 · 이상치 판정 | §5.1 `brk_cum`, §5.3 `burn_suspect`, §5.5 `active`, §5.6 `stagnation_start_level`, §5.8 `growth_rate`. **§2.1 이상치**(`anomaly_level_drop` 결측화, `cp_drop`·`anchor_mismatch` `note`). |
| 5 | Q1 돌파·정착 | §5.1 돌파율(`approach` 260도달·`at260` 261이탈), §5.4 정착률(버닝추정 vs 비버닝), §5.7 부트스트랩+Fisher, (여력 시 §5.2). → `breakthrough_retention_summary.csv` 기록. |
| 6 | Q2 레벨대별 리텐션 | §5.5 리텐션 곡선 4코호트 + §5.8 일간 성장률 곡선. 이동평균·CI 포함. |
| 7 | Q3 정체 지도 | §5.6 풀링 → `stagnation_pooled.csv` 기록. 258~260 / 280~281 집중도 수치. |
| 8 | 시각화 4종 | §7 스펙대로 `images/*.png` 저장. |
| 9 | 인사이트 (markdown) | 3개 이상. 각 인사이트를 **관찰(Fact — 수치·구간)** / **원인(Why — 가설 1~2, 반례 가능)** / **행동(Action — 다음 분석 또는 계획문서 §9 실무 제언 연결)** 3단 (미션 §8, 계획문서 §1). |

---

## 7. 시각화 4종 스펙

공통: `matplotlib`, `figsize=(9,5)`, `dpi=150`, 한글 폰트(`Malgun Gothic`), 제목·축라벨 한국어, 범례 우상단.

**시각화 가이드 (미션 §8):**
- **축**: x축은 `elapsed_day`(0~24, 일 단위 균등). 날짜 정렬·누락일 점검은 §6 셀 2에서 선행.
- **스케일**: 돌파율·리텐션 y축 `0~1` 고정. 레벨 궤적은 실측 범위, 전투력은 로그 검토 후 캡션 명시.
- **집계 단위**: 각 그래프 캡션에 "집계 단위 = 앵커일 기준 경과일(일간), 창=25일" 1줄.

| 파일 | 차트 | x축 | y축 | 시리즈 | CI / 강조 |
|---|---|---|---|---|---|
| `01_breakthrough_curve.png` | 선그래프 | 경과일 0~24 | 260 돌파율 (0~1) | `approach`(260 도달률), `at260`(261 이탈률) | 부트스트랩 CI `fill_between` |
| `02_retention_by-level-tier.png` | 선그래프 | 경과일 | 리텐션율 (0~1) | `approach`/`at260`/`past260`/`burnend` | CI 음영. `at260` 강조색 |
| `03_stagnation_map.png` | 히스토그램 | 정체 시작 레벨 (bin=1, 250~300) | 캐릭터 수 | 4코호트 풀링(코호트별 색 스택) | `axvspan` 으로 258–260·280–281 강조 |
| `04_growth_trajectory.png` | 스파게티(선) | 경과일 | 레벨 | 캐릭터별 얇은 선 (`approach`+`at260`) | `burn_suspect` 만 진한 색, 나머지 회색. y=260 수평선 |

---

## 8. `REPORT.md` 목차

1. **분석 주제 및 선정 이유** — 계획문서 §1·§2 요약.
2. **분석 질문 (3개 이상)** — 계획문서 §4 핵심 질문 + 세부질문 1–3.
3. **데이터 설명** — 출처(NEXON Open API), 기간(앵커 2026-06-18 ~ +24일), 4코호트 표(계획문서 설계 변경 2),
   데이터 수(4코호트 × 100명 × 25일 ≈ 10,000행). **라이선스 주의 문구**(§2).
   **시계열 특성** 문단(트렌드/계절성 해당 없음/노이즈, §6 셀 3.5 요약).
4. **데이터 정제** — 결측치·이상치 처리 기준(§2.1 표 요약), 실제 제외/보정 건수, `n_dropout` 코호트별 수치.
5. **분석 기법과 적용 이유** — 이동평균·구간별 통계·변화율(§5.8)·부트스트랩 CI 를 왜/어떻게 썼는지 각 1~2줄.
6. **결과 Q1 · 돌파와 정착** — 260 도달률(`approach`)·261 이탈률(`at260`)·정착률. 각 소절 **관찰(수치)** → **해석(가설)** 분리.
7. **결과 Q2 · 레벨대별 리텐션 + 일간 성장률**
8. **결과 Q3 · 정체 시작 지점 지도** — 260 플래토(약 100만 캐릭터) 규모를 핵심 관찰로.
9. **인사이트 (3개 이상)** — 각 인사이트 = 관찰(Fact) / 원인(Why) / 행동(Action) 3단 (미션 §8).
10. **결론** — 핵심 질문("260까지 키우면 계속 키우나 방치하나 / 이벤트가 이를 어떻게 바꾸나")에 대한 답 3~5줄 요약.
11. **한계점** — 계획문서 §12 전체 + (a) 5/1≈6/18 라 이벤트 순효과 분리 약함 (b) 관측 25일로 30일 이후 미관측 (c) 랭킹 미진입 초저활동 유저 배제.
12. **AI 사용 로그** — 계획문서 §11 3항목(사용 작업 / 사용 이유 / 검증 방법)을 단계별로.
13. **부록 · 재현 방법** — 환경변수 설정, `python collect.py`, 노트북 실행 순서 (README 와 동일 내용).

---

## 9. 미션 요구사항 ↔ 스펙 매핑

| 미션 요구사항 | 충족 위치 |
|---|---|
| 시계열 데이터 1개, 100+ 포인트 | §4.1 — 4코호트 × 100명 × 25일 ≈ 10,000행. 코호트 일간 평균 = 25점 시계열 4개 |
| 출처·기간·라이선스 명시 | §2 라이선스 문구, §8 REPORT 3절, §1.1 README |
| 분석 질문 3개 이상 | §8 REPORT 2절 (계획문서 §4 세부질문 1–3) |
| 데이터 기본정보(기간·컬럼·결측) 확인 | §6 셀 2 |
| 결측치/이상치 기준 정해 처리 | §2.1 규약, §6 셀 3·4, §8 REPORT 4절 |
| 시계열 트렌드/계절성/노이즈 개념 설명 | §6 셀 3.5, §8 REPORT 3절 (일간 25일이라 요일별 평균으로 계절성 점검 후 기각) |
| 시계열 분석 기법 2가지 이상 | §5.7·§5.8 — 이동평균·변화율·구간통계·부트스트랩 4종. 왜/어떻게는 §8 REPORT 5절 |
| 시각화 2개 이상(3개+ 권장) | §7 (4종), 축·스케일·집계단위 가이드 포함 |
| 인사이트 3개 이상, 관찰 근거 포함 | §6 셀 9 — 관찰(Fact)/원인(Why)/행동(Action) 3단, §8 REPORT 9절 |
| 관찰(근거) / 해석(가설) 구분 서술 | §6 셀 9, §8 REPORT 6–8절 소제목 규칙 |
| REPORT: 주제·질문·데이터·시각화·인사이트·결론·한계 | §8 REPORT 1·2·3·6–8·9·10·11절 |
| requirements.txt / 실행 방법 | §1, §1.1 README, §8 REPORT 13절 |
| GitHub 저장소 (코드·리포트·데이터/문서) | §1 구조 + §1.1 (README·.gitignore, data 커밋) |
| AI 사용 로그 3항목 | §8 REPORT 12절 (계획문서 §11) |
| Python 3.10 이상 | §1 |
| (보너스) 대시보드 / 시계열 분해·예측 | §10 — 시계열 분해는 여력 시 시도(일간 25점), 대시보드는 계획문서 §13 로드맵 |

---

## 10. 구현 체크리스트

- [x] `collect.py` — §3. API 파라미터(`world_type=0`, page 200행, 전투력 키) 실호출 확인 완료.
- [ ] `python collect.py` 반복 실행(~12일) → `data/cohort_{approach,at260,past260,burnend}.csv` (각 ≤2,500행).
- [ ] `analysis.ipynb` — §6 셀 1~9(중 3.5 markdown). `breakthrough_retention_summary.csv`, `stagnation_pooled.csv` 생성.
- [ ] `images/*.png` 4개 — §7 (x축 경과일, y 0~1 고정, 캡션 근거).
- [ ] `REPORT.md` — §8 13절 목차. 결측/이상치·시계열 특성·인사이트 3단·결론·라이선스·한계·AI 로그 포함.
- [x] `requirements.txt` — §1.
- [x] `README.md` · `.gitignore` · `.env.example` — §1.1.

### 보너스 (미션 §5)

- 시계열 분해: 일간 25점이 생기므로 `approach`/`at260` 코호트 평균 곡선에 대해 추세-잔차 분해를 **여력 시 시도**
  (계절성은 25일이라 제한적). 예측은 범위 밖.
- 대시보드: 계획문서 §13 로드맵(9/16 이벤트 종료 후 Streamlit)으로 이관.

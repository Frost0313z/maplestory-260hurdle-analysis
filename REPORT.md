# 메이플스토리 "260 허들" 분석 리포트

작성일: 2026-09-03 · 데이터: NEXON Open API · 코드: `collect.py` → `analysis.ipynb` → 이 문서
짝 문서: `메이플스토리_이벤트분석_계획문서.md`(왜/무엇) · `메이플스토리_이벤트분석_스펙문서.md`(어떻게)

---

## 1. 분석 주제 및 선정 이유

메이플스토리는 레벨 210·260에서 경험치 요구량이 급증하고 261부터 고정된다. 특히 **레벨 260**은
콘텐츠 난이도와 육성 편의(하이퍼 버닝 MAX가 200~260 구간을 커버, 버닝 BEYOND가 260~280을 커버)의
경계로, 커뮤니티에서 "허들"로 불린다. 2026 여름 성장 이벤트(2026-06-18~09-16) 진행 중이라
"이벤트가 이 허들 돌파를 얼마나 바꾸는가"를 관찰할 수 있는 시점이다.

**핵심 질문**: 유저는 레벨 260까지 키운 캐릭터를 계속 키우는가, 방치하는가? 여름 성장 이벤트는
이 패턴을 얼마나·어떻게 바꾸는가?

선정 이유: (a) 넥슨 오픈 API로 레벨/전투력 시계열을 소급 수집 가능, (b) 260이라는 명확한 임계값으로
"돌파율"을 이분법 지표로 설계 가능, (c) 라이브 서비스 지표 해석 역량을 보이는 포트폴리오 목적.

---

## 2. 분석 질문

**핵심 질문** — 260까지 키운 캐릭터를 계속 키우나 방치하나 / 이벤트가 이를 어떻게 바꾸나.

- **세부질문 1 · 돌파와 정착**: (a) 260 돌파율이 성수기(이벤트 기간)에 얼마나 되는가?
  (b) 돌파가 정착(돌파 후 활동 지속)으로 이어지는가, 아니면 반짝 성장 후 방치되는가?
- **세부질문 2 · 방치가 260 이전부터 시작되는지**: 성장 여지가 남은 코호트끼리도 레벨대에 따라
  활동 지속률(리텐션)이 다른가?
- **세부질문 3 · 방치가 시작되는 지점이 정말 260인지**: 200~300 범위에서 정체가 몰리는 레벨은 어디인가?

> 분석 도중, "레벨 X 에 정지해 있던" 표본은 260을 향해 *오르던* 인구를 배제한다는 설계 결함을
> 발견해, 앵커 직전 레벨 상승분으로 스크리닝한 `climb` 코호트를 추가 수집했다(설계 변경 4). 세부질문 1은
> 이 코호트로 다시 측정한다(§6).

---

## 3. 데이터 설명

- **출처**: NEXON Open API (`https://open.api.nexon.com/maplestory/v1`). `/id`, `/character/basic`,
  `/character/stat`, `/ranking/overall`.
- **기간**: 앵커일 **2026-06-18**(여름 성장 이벤트 시작) ~ **2026-07-22**(+34일), 일간 소급 스냅샷 35일.
  전투력은 D+0/8/16/24/32 5개 시점만.
- **표본 (5코호트, 앵커일 통합 랭킹 `world_type=0` 소급 고정 → 생존자편향 방지)**:

  | 코호트 | 앵커일 실측 레벨 | 중심 page(≈rank) | 표본 n | 역할 |
  |---|---|---|---|---|
  | `approach` | 251 | 21,000 (≈4.2M) | 300 | 허들 접근 — 35일 안에 260에 닿는가 |
  | `at260` | 260 | 15,000 (≈3.0M) | **800** | 260 정체 플래토 — 탈출률 (대형 표본) |
  | `past260` | 262 | 10,000 (≈2.0M) | 300 | 260 직후 — 넘긴 뒤 계속 크는가 |
  | `burnend` | 281 | 2,500 (≈0.5M) | 300 | 버닝 BEYOND 종료 지점 |
  | `climb` | 256 | 18,400 (≈3.7M) | 300 | **허들 등반** — 앵커 직전 14일간 레벨이 오른(모멘텀) 캐릭터만. "실제로 오르던 인구"가 260을 넘는가 (설계 변경 4) |

  > `approach`~`burnend` 는 "앵커일에 레벨 X 에 정지해 있던" 인구라, 260을 향해 *오르고 있던* 캐릭터를
  > 구조적으로 배제한다(레벨 X 는 활동 유저에게 통과 지점, 방치 유저에게 주차 지점). `climb` 는 앵커 직전
  > 레벨 상승분(2026-06-04 대비)을 스크리닝해 이 결함을 보정한 코호트다 (§6 참고).

- **데이터 수**: 5코호트 × 표본 × 35일 = **70,000행** (`at260` 28,000 + 나머지 각 10,500).
  코호트 일간 평균 = 35점 시계열 5개. API 호출 약 73,600건 (락 에러 0).
- **라이선스**: 본 분석은 NEXON Open API 이용약관을 준수한다. 비상업적 포트폴리오 용도이며,
  데이터 출처를 "NEXON Open API"로 표기한다. 재배포·상업 이용 전 약관 재확인 필요.
  `data/*.csv`는 넥슨 공개 랭킹 기반 데이터로 재현성을 위해 저장소에 포함한다.

### 시계열 특성 (트렌드 / 계절성 / 노이즈)

- **트렌드**: `approach`·`at260`·`past260` 세 코호트의 평균 레벨·돌파율은 35일간 **사실상 수평**
  (일간 성장률 0.0002~0.0009 레벨/일). `burnend`만 완만한 우상향(0.003 레벨/일). 대부분 코호트에서
  "성장 트렌드"라 부를 신호가 거의 없다는 것 자체가 핵심 관찰이다.
- **계절성**: 관측 35일(=5주). 요일별 평균 활동률에서 주말(금~일 2.8%)이 평일(화~목 0.07%)보다
  높은 약한 주간 패턴이 있으나, 전 코호트 활동률 자체가 6% 미만이라 실질 영향이 없다 → **기각**.
- **노이즈**: 개별 캐릭터 궤적의 소수 진동(전투력 리롤 등)과 표본 크기(코호트당 유효 283~774명).
  코호트 곡선은 이동평균(window=3), 비율 지표는 부트스트랩 95% CI를 함께 제시해 대응했다.

---

## 4. 데이터 정제

**결측 (조회 실패) — 스펙 §2.1 규약**: 조회 실패는 "무활동(0)"이 아닌 **별도 이탈 신호**로 분리,
비율 지표 분모에서 제외하고 `n_dropout`으로 집계.

| 사유 | `error_code` | approach | at260 | past260 | burnend |
|---|---|---|---|---|---|
| 계정삭제·개명 등 (ocid 조회 실패) | `ocid_failed` | 8명 | 20명 | 10명 | 4명 |
| ocid는 유효하나 basic이 전 기간 400 (조회불가 계정) | `http_400` | 3명 | 6명 | 6명 | 0명 |
| **완전 이탈 캐릭터 계** | | **11** | **26** | **16** | **4** |
| 유효 표본 (분석 대상) | | 289 | 774 | 283~284 | 295~296 |

`climb` 코호트(n=300)는 앵커·2026-06-04 두 시점을 스크리닝 단계에서 이미 확인한 캐릭터만 수집해
**이탈 0명** (유효 300).

- `http_400`은 스펙 §2.1 표에 없던 코드다. ocid는 정상 발급되나 `/character/basic`이 35일 내내
  HTTP 400을 반환하는 계정으로(15명/525행), 실질적으로 조회 불가이므로 `http_404`와 동일하게
  이탈 신호로 처리했다.
- 이탈률은 코호트별 1.4~5.7%로, "랭킹 미진입 초저활동 유저 배제"와 함께 생존자편향의 일부로 남는다.

**이상치 — 스펙 §2.1 규약** (노트북 파생 판정, 원본 CSV 불변):

| 패턴 | 판정 | 결과 |
|---|---|---|
| 레벨 감소 (`level_t < level_{t-1}`) | 물리적 불가 → 결측 처리 | **0행** |
| 전투력 직전 대비 −30% 초과 하락 | 정상 행위(장비 해제·리롤) → 이상치 아님, `note`만 | 52행 (값 유지) |
| 앵커(D+0) 레벨이 코호트 목표대와 크게 다름 | 표본 오추출 의심 | **0명** (D+0 레벨: `approach`/`at260`/`past260`/`burnend` = 251/260/262/281 정확히 일치, `climb` = 256 (1명만 257)) |

앵커일 스냅샷을 직접 수집했으므로 캐릭터별 실제 시작 레벨이 CSV에 있고, 전 코호트 목표 레벨과
편차 0(`climb`은 ±1)으로 확인되어 `anchor_mismatch` 표본 제외는 없었다.

---

## 5. 분석 기법과 적용 이유

| 기법 | 어디에 | 왜 |
|---|---|---|
| **이동평균 (window=3, 중심)** | 리텐션·성장률 코호트 곡선 | 개별 궤적 진동·조회 일시 실패로 인한 노이즈 완화 |
| **변화율 (일간 레벨 증가율)** | 세부질문 2·3, 성장 궤적 | 성장 속도가 꺾이는 시점/레벨 파악, 버닝 정황 보강 |
| **구간별 통계 비교** | 코호트 간 (approach/at260/past260/burnend + 모멘텀 표본 climb) | 레벨대·성장도에 따른 활동성 차이, 그리고 "정지 인구"와 "등반 인구"의 대비 정량화 |
| **부트스트랩 95% CI** (`scipy.stats.bootstrap`, percentile, n_resamples=10,000) | 돌파율·리텐션·정착률 등 비율 지표 | 표본이 작을수록 넓어지는 구간이 표본 크기의 한계를 정직하게 드러냄. 분자가 0이거나 표본<2면 점추정 반환(축퇴 구간 명시) |
| **Fisher's exact test** (`scipy.stats.fisher_exact`) | approach 버닝추정 × D+34 260돌파 | 소표본 2×2 유의성. **단 버닝추정군 n=0으로 계산 불가** (§6 참고) |

---

## 6. 결과 Q1 · 돌파와 정착

### 관찰 (수치)

- **260 돌파율(`approach`, 260 도달)** D+34: **0.0035** (289명 중 1명), 95% CI [0.0000, 0.0104].
  35일 내내 곡선이 바닥에서 벗어나지 않는다. (`images/01_breakthrough_curve.png`)
- **261 이탈률(`at260`, 플래토 탈출)** D+34: **0.0039** (774명 중 3명), 95% CI [0.0000, 0.0090].
- **버닝추정 플래그**(`approach`·`at260`, D+7 내 260 도달 OR 관측 중 최대 레벨 − D+0 레벨 ≥ 15):
  **양 코호트 통틀어 0명.** `at260`은 D+0 레벨이 260이라 스펙의 "D+7 내 260 도달" 절이 자명 참이
  되므로 "Δ레벨 ≥ 15" 절만 유효하며, 그 기준으로 해당자가 없다.
- **정착**(돌파 후 남은 관측 중 최소 1회 레벨/전투력 변화) — 분모가 작아 원자료로 표기:

  | 코호트 | 돌파자(정착 판정 가능) | 정착 | 비고 |
  |---|---|---|---|
  | `approach` | 1 | 0 | 유일한 260 도달자는 이후 활동 없음 |
  | `at260` | 3 | 2 | 261 이탈 3명 중 2명 계속 성장 |
  | `past260` (이미 262) | 284 | 61 (21.5%) | "넘긴 뒤 계속 크는가" = 전 구간 활동 여부 |
  | `burnend` (이미 281) | 296 | 187 (63.2%) | 〃 |

- **Fisher's exact test**: approach 버닝추정군 n=0으로 2×2가 성립하지 않는다 →
  **"버닝추정군 vs 비버닝군 정착률 비교"(계획문서 세부질문 1의 핵심 산출값)는 수행 불가.**

### `climb` — 모멘텀 코호트로 본 "진짜" 돌파율 (설계 변경 4)

`approach`(Lv.251 고정)는 등반자를 구조적으로 배제한다. 앵커 직전 14일간 레벨이 오른(2026-06-04
대비) Lv.255~258 캐릭터 300명(`climb`, 이탈 0)으로 다시 측정하면:

| 지표 | `climb` (모멘텀 표본) | 대조: `approach` |
|---|---|---|
| 35일 내 **260 도달** | **18.0%** (54/300) | 0.3% (1/289) |
| 261+ 도달 | 1.7% (5/300) | — |
| **270+ 도달** (BEYOND 상한 근처) | **0명** | — |
| 35일간 레벨 무변동 | 69% (208/300) | 99.7% |

**260 도달자 54명의 260 이후 최종 레벨** (260 최초 도달일 중앙 D+23):

| 최종 레벨 | 인원 |
|---|---|
| (a) 260 유지 | **49명 (91%)** |
| (b) 261~269 | 5명 (9%) |
| (c) 270+ | **0명 (0%)** |

**robustness check — "늦게 260을 찍어 성장할 시간이 없었을 뿐" 반론 검정**
(관측 종료 D+34 − 최초 260 도달일 = post-260 관측창):

| post-260 관측창 | n | 최종 260 | 261+ | 262+ | 270+ |
|---|---|---|---|---|---|
| ≥ 0일 (전체) | 54 | 49 (**91%**) | 5 (9%) | 3 | 0 |
| **≥ 7일** | 39 | 35 (**90%**) | 4 (10%) | 2 | 0 |
| **≥ 14일** | 23 | 20 (**87%**) | 3 (13%) | 2 | 0 |

- 관측창을 ≥14일로 좁혀도 87%가 260에 남는다 → **관측 기간 부족으로 인한 착시가 아니다.**
- 261 도달자는 5명, 260→261 소요일 median 5일. 270+는 어느 층에서도 0.
- 단 "즉시 정지"는 아니다: ≥14일 관측된 조기 도달자 중 260에 남은 20명의 **마지막 `exp_rate`
  중앙값은 26.6%** (max 47%). 260 도달 시점 `exp_rate` 중앙 1.4%에서 14일+ 동안 20%p 이상
  누적했으나 **레벨 경계(261 요구량)를 넘지는 못한 상태**다. (`images/06_climb_trajectory.png`)

### 관찰 → 해석 분리

**FACT (데이터가 직접 말하는 것)**: 성수기 앵커에서, 모멘텀 스크리닝을 통과한 등반자조차
35일 내 260 도달은 18%, 그중 **260→261 전환율이 매우 낮다** (관측창 ≥14일 확보 시에도 13%).
270+ 도달은 0. 260에 남은 캐릭터의 일부는 완전 정지가 아니라 260 내부에서 느리게 진행 중이다.

**메커니즘 (가설, 데이터로 인과 확정 불가)**: 260 도달 이후 상당수가 261로 전환되지 못하며,
충분한 관측 기간을 확보한 경우에도 성장 속도가 다음 레벨 요구량을 넘을 정도로 유지되지 않는다.
왜 261 전환이 느린지에 대해서는 서로 배타적이지 않은 여러 게임 구조가 후보다 —
① 하이퍼 버닝 MAX의 성장 목표 자체가 260이라 이벤트 캐릭터에게 260이 명시적 완료 지점,
② 유니온(리전)용 부캐 육성이 특정 레벨에서 목적을 달성,
③ 버닝 BEYOND는 260에서 지정할지 판단하는 분기점(261에서 지정 시 요구 경험치 증가).
현재 표본으로는 이 중 무엇이 지배적인지, 260 정지가 능동적 선택인지 계정 이탈인지 구분할 수 없다.

관측 창(35일) 안에서 270+ 도달이 0인 것은 (a) `climb` 표본에 BEYOND 지정 캐릭터가 없거나
(b) 260 도달이 늦어(중앙 D+23) BEYOND 폭주가 창 밖에서 일어난다 — 둘을 구분할 수 없다(§11).

---

## 7. 결과 Q2 · 레벨대별 리텐션 + 일간 성장률

### 관찰 (수치)

- **활동성 base-rate** (35일간 레벨·exp·전투력 중 하나라도 변동한 캐릭터 비율):

  | 코호트 | 레벨 변동 | exp 변동 | 전투력 변동 | **활동(합집합)** | 유효표본 |
  |---|---|---|---|---|---|
  | `approach` (251) | 0.3% | 2.1% | 9.0% | **9.0%** | 289 |
  | `at260` (260) | 0.4% | 3.4% | 11.9% | **12.0%** | 774 |
  | `past260` (262) | 0.4% | 10.9% | 21.5% | **21.8%** | 284 |
  | `burnend` (281) | 9.8% | 47.0% | 63.2% | **63.9%** | 296 |

- **8일 구간 활동률** (직전 체크포인트 대비 레벨 또는 전투력 변화, `images/02_retention_by-level-tier.png`):

  | 경과일 | approach | at260 | past260 | burnend |
  |---|---|---|---|---|
  | D+8 | 3.8% | 5.8% | 12.7% | 51.7% |
  | D+16 | 3.1% | 4.1% | 11.7% | 49.3% |
  | D+24 | 2.4% | 4.7% | 11.7% | 44.1% |
  | D+32 | 2.4% | 2.8% | 8.5% | 35.6% |

  → 코호트 순서대로 뚜렷한 **활동성 구배**가 있고, 모든 코호트에서 시간이 갈수록 완만히 **하락**한다
  (버닝 활동 코호트 `burnend`도 52%→36%).
- **일간 성장률**: `burnend` 0.0031 레벨/일, 나머지 0.0002~0.0009 레벨/일.
- **전투력·성장도 구배** (D+0 전투력 중앙값): `approach` 17.4만 → `at260` 140만 → `past260` 482만
  → `burnend` 2,954만 (약 170배). 코호트 내부에서도 활동 캐릭터의 전투력이 정지 캐릭터의 1.5~2배.
- **일간 `retention_rate` 주의**: `data/breakthrough_retention_summary.csv`의 `retention_rate` 컬럼은
  전투력이 8일 간격으로만 관측되어 D+8/16/24/32에만 값이 튀는 **빗살 형태**다. 레벨대 비교는 위
  "8일 구간 활동률"로 수행했다.

### Q2 심화 — 전투력 성장 "규모" (`images/05_combat_power_growth.png`)

위 활동률은 전투력 변화를 이분값(변함/안 변함)으로 본 것이라 장비 리롤·해제도 "활동"으로 잡힌다.
5개 전투력 스냅샷의 **실제 크기 변화**(D+0→D+32, D+0 전투력>0 캐릭터)를 보면:

| 코호트 | D+0 전투력 0(무장비) | 전투력 +10%↑ | **실질 성장률** (레벨↑ 또는 전투력↑, 리롤 제외) |
|---|---|---|---|
| `approach` (251) | 20/289 | 4명 | **5%** |
| `at260` (260) | 76/774 | 17명 | **6%** |
| `past260` (262) | 25/283 | 11명 | **10%** |
| `burnend` (281) | 11/295 | 57명 | **32%** |

- 활동을 "실제 성장"으로 좁히면 구배가 더 선명해진다. **251·260·262는 사실상 구분되지 않고(5~10%),
  281에서만 절벽**이 나타난다. `at260`의 **10%가 전투력 0** — 순수 레벨 셸(장비 없이 레벨만 올린 캐릭터).
- `burnend` 딥다이브: 29명 레벨업(281→282~283), 전투력 +25%↑ 20명(상위 +227%·+148%). 8일 구간별로
  활동 캐릭터가 줄어든다(CP 변화 캐릭터 D+0→8 구간 153명 → D+24→32 구간 105명, 정지 131→179명).

### 해석 (가설)

활동성을 가르는 것은 레벨대 자체보다 **캐릭터 성장도(전투력)**로 보인다. 낮은 전투력 = 장비 투자
없이 레벨만 올린 캐릭터이며, 전투력이 높은(본캐로 육성된) 캐릭터일수록 이벤트 기간에도 계속
성장한다. "허들에 가까울수록 리텐션이 가파르게 하락"할 것이라는 계획문서 가설과 달리, 250 근방이
특별히 낮은 게 아니라 **성장도·랭킹 깊이를 따라 단조롭게** 낮아진다. 즉 레벨 기반 세그먼트보다
성장 투자 지표 기반 세그먼트가 리텐션 타겟팅에 유효하다는 시사점(계획문서 세부질문 2의 대안 결과).

---

## 8. 결과 Q3 · 정체 시작 지점 지도

### 관찰 (수치)

- **정체 시작 레벨**(연속 3일 이상 레벨 불변이 처음 시작되는 레벨, 4코호트 풀링,
  `images/03_stagnation_map.png` · `data/stagnation_pooled.csv`):

  | 정체 시작 레벨 | 캐릭터 수 |
  |---|---|
  | 250 | 1 |
  | **251** | 289 |
  | **260** | 772 |
  | **262** | 284 |
  | **281** | 296 |

- 258~260 구간 집중: 772명(관측된 정체자의 47%) — 전부 `at260` 코호트.
- 280~281 구간 집중: 296명(18%) — 전부 `burnend` 코호트.
- 관측 내내 성장을 지속해 정체 시작이 없는 캐릭터: 1,643명 중 1명(`burnend`).

### 해석 (가설)

정체 시작 레벨 분포는 각 코호트가 앵커일에 자리 잡은 레벨(251/260/262/281)에 그대로 몰린다.
이는 **"정체 지점을 데이터로 발견"했다기보다 "그 레벨대에 있던 캐릭터가 거의 움직이지 않는다"**를
확인한 것에 가깝다(계획문서도 `at260` 플래토는 자연 집계됨을 명시). 다만 두 가지가 관찰된다:

1. **`at260`(260)와 `past260`(262)의 정지 강도가 사실상 동일** — 260 "직전"과 "직후"의 차이가 없다.
   허들이 260에 날카롭게 존재한다면 262는 달라야 하는데 그렇지 않다.
2. **정지 지점의 `exp_rate`(레벨 내 진행도, D+0 기준)**: `approach` 중앙 0.1%(전원 <1%),
   `at260` 전원 <10%, `past260` 중앙 1.9% — 세 코호트 모두 **레벨을 막 찍은 상태에서 멈췄다**.
   반면 `burnend`는 중앙 43.8%로 레벨 중간중간에 흩어져 있다(유기적 플레이).

레벨 경계에서 진행도 ≈0으로 멈추는 패턴은 "며칠 갈다가 콘텐츠 난이도에 막혀 후퇴한 것"과는
다르다 — 레벨을 찍은 직후 진행이 거의 멈춘다. `climb` 코호트(모멘텀 표본)에서도 260 도달자의
87~91%가 261로 전환되지 못한다(§6, 관측창 층화 후). 계획문서가 가정한 두 메커니즘(258~260 콘텐츠
난이도, 280~281 버닝 종료)에 더해, 261 전환이 느린 이유의 후보로 다음 두 가지를 추가한다
(서로 배타적이지 않으며 데이터로 인과 확정 불가).

- **③ 유니온(리전) 목적 달성 후 파킹**: 유니온은 부캐 레벨 합이 본캐 스펙을 올리므로,
  버닝으로 250·260 근방 브레이크포인트까지 올린 뒤 더 올리지 않는 플레이가 일반적. 260을 넘기면
  경험치 요구량은 급증하고 유니온 한계효용은 줄어 넘길 유인이 약하다.
- **④ 버닝 BEYOND 지정 분기점**: BEYOND(260→상한)는 260 시점에 지정 여부를 판단하며, 261 이후
  지정 시 요구 경험치가 늘어난다. 지정을 염두에 둔 캐릭터라면 260에서 진행을 늦출 유인이 있다.
  `at260` 플래토가 정확히 260에 쌓이는 것, `climb` 도달자가 260에서 261로 잘 넘어가지 않는 것이
  이 구조와 상충하지 않는다 — 다만 표본에 실제 BEYOND 지정 캐릭터가 얼마나 있는지는 알 수 없다.

---

## 9. 인사이트

### 인사이트 1 · "260 이후 방치"의 상당 부분은 실패가 아니라 설계된 종착점

- **관찰(Fact)**: `approach`(251)·`at260`(260)·`past260`(262) 세 코호트 모두 35일 활동률 9~22%,
  레벨 변동 캐릭터는 각 1명 이하. 정지 캐릭터의 D+0 `exp_rate`는 세 코호트 모두 <10%. **모멘텀
  표본(`climb`)에서도 260 도달자의 87~91%가 261로 전환되지 못한다** (관측창 ≥14일 확보 후에도),
  270+ 도달 0명. `burnend`(281)만 `exp_rate` 중앙 43.8%, 활동 64%로 질적으로 다르다.
- **원인(Why, 가설)**: ① 유니온(리전)은 부캐 레벨 합이 본캐 스펙을 올리는 구조라, 버닝으로 특정
  레벨까지 올린 뒤 더 올리지 않는 플레이가 일반적 — 260 플래토의 상당수는 "막힌 유저"가 아니라
  **목적을 달성한 유니온 인프라**일 수 있다. ② 하이퍼 버닝 MAX의 성장 목표가 260이라 이벤트
  캐릭터에게 260이 명시적 완료 지점. ③ 버닝 BEYOND 지정 여부를 260에서 판단. (④ 일부는 실제
  탈퇴 계정 — 스냅샷으로 능동적 파킹과 구분 불가.)
- **행동(Action)**: "260 돌파율"을 단독 리텐션 KPI로 쓰면 안 된다. 260 정지의 상당 부분이 실패가
  아닐 수 있으므로, 정체를 일률적으로 "이탈 위험"으로 보지 말고 260 이후 상시 progression
  전환(6차·HEXA·심볼·장비 성장, BEYOND 지정 후 상한 도달) 지표를 병행해야 한다.

### 인사이트 2 · 활동성을 가르는 건 레벨대가 아니라 캐릭터 성장도(전투력)

- **관찰(Fact)**: D+0 전투력 중앙값이 코호트 순서대로 17.4만 → 140만 → 482만 → 2,954만(약 170배).
  활동률도 같은 순서로 9→12→22→64%. 코호트 내부에서도 활동 캐릭터의 전투력이 정지 캐릭터의 1.5~2배.
- **원인(Why, 가설)**: 낮은 전투력 = 장비 투자 없이 레벨만 올린 캐릭터 = 유니온용 부캐일 개연성.
  "260 허들"은 난이도 장벽이라기보다 **본캐/부캐 성격의 프록시**로 작동한다. 250 근방이 특별히
  낮은 게 아니라 성장도·랭킹 깊이를 따라 단조롭게 낮아진다.
- **행동(Action)**: 레벨 기반 세그먼트(계획문서 세부질문 2)의 실효성이 낮다. 전투력·과금·유니온
  레벨 등 **성장 투자 지표**로 세그먼트를 재정의해야 리텐션 타겟팅이 유효하다.

### 인사이트 3 · 여름 성장 이벤트는 이미 자리 잡은 캐릭터를 되살리지 못한다

- **관찰(Fact)**: 성수기 앵커임에도 `approach` 260 도달률 D+34 ≈ 0.0035(1/289). 모멘텀 표본(`climb`)조차
  260 도달 18%, 그중 87~91%가 261로 전환 실패(관측창 층화 후), **270+ 도달 0명**. 버닝추정군은
  `approach`·`at260` 통틀어 0명 → "버닝추정 vs 비버닝 정착률 비교"는 수행 불가.
- **원인(Why, 가설)**: 하이퍼 버닝·BEYOND는 계정당 1캐릭터만 지정 가능. 이미 자리 잡은 캐릭터도,
  BEYOND 슬롯을 다른 캐릭터에 쓴 `climb` 캐릭터도 이벤트 효과가 도달할 경로가 없다. 관측 35일
  안에서 BEYOND 폭주가 한 명도 없는 것은 (a) 표본이 BEYOND 미지정 (b) 260 도달이 늦어(중앙 D+23)
  폭주가 창 밖 — 둘을 구분 못 한다.
- **행동(Action)**: 이벤트 ROI를 "기존 정체 캐릭터 재활성화"로 기대하면 안 된다. 이벤트의 실제
  타겟은 신규·저레벨 캐릭터 생성이며, 그 지표(신규 캐릭터 생성 수, BEYOND 지정률, 지정 후 280 도달
  시간)로 성과를 측정해야 한다.

> 관찰은 모두 넥슨 API 레벨/전투력 스냅샷 기반 대리지표다. 유니온 소속·계정 연결·실접속 로그는
> API로 확인 불가하며, 위 원인은 정황 근거에 기반한 가설이다.

---

## 10. 결론

**핵심 질문 — 260까지 키운 캐릭터를 계속 키우나 방치하나 / 이벤트가 이를 어떻게 바꾸나:**

1. 랭킹 통합 3M위 근방(레벨 251~262)에 자리 잡은 캐릭터는 **35일간 99% 이상이 레벨을 전혀 올리지
   않는다.** 이 "정체"의 상당 부분은 실패가 아닐 수 있다 — 유니온 목적을 달성했거나, 하이퍼 버닝
   MAX의 목표점(260)에 도달했거나, BEYOND 지정을 260에서 판단 중인 캐릭터가 섞여 있다.
   다만 능동적 파킹과 계정 이탈은 스냅샷으로 구분할 수 없다.
2. **여름 성장 이벤트는 이 코호트를 거의 움직이지 못한다.** 260 도달률은 성수기에도 ~0.3%이고,
   실제로 오르던 인구(`climb`)조차 18%만 260에 닿으며 그중 260→261 전환은 관측창을 충분히
   확보해도 13%에 그친다. 270+ 도달은 0명. 버닝은 계정당 1캐릭터 한정이라 이미 자리 잡은
   캐릭터에는 효과가 닿기 어렵다.
3. 계속 키우는 캐릭터는 **레벨대가 아니라 전투력(성장 투자)이 높은 캐릭터**다. 활동률은 레벨 251→281이
   아니라 전투력 17만→3천만을 따라 9%→64%로 올라간다. 실질 성장률로 좁히면 251·260·262가 5~10%로
   구분되지 않고 281에서만 32%로 절벽. `burnend`(281)조차 35일에 걸쳐 활동률이 52%→36%로 하락한다.
4. 따라서 "260 허들"은 **순수 난이도 장벽이라기보다 버닝 시스템 안에서 260이 성장 목표점으로
   작동**하는 데 가깝다 (유니온 부캐 생애주기, 하이퍼 버닝 목표 레벨, BEYOND 지정 분기). "260
   돌파율" 단독 개선을 목표로 삼는 대신, **260 이후 상시 progression 전환율**을 핵심 지표로 봐야
   한다. 누가 전환하고 누가 안 하는지, 그 차이를 조기에 식별할 수 있는지가 Part 2의 질문이다(§14).

---

## 11. 한계점

- **대리지표**: "리텐션"·"돌파"·"정착"은 실접속 로그가 아닌 레벨/전투력 스냅샷 기반이다.
- **계정 연결 불가**: 넥슨 오픈 API는 계정-캐릭터 연결을 제공하지 않는다. "본캐/부캐", "유니온 부캐"는
  **측정값이 아니라 전투력·랭킹 깊이·정지 패턴에 기반한 해석 가설**이다. "의도된 파킹(소유자 활동중)"과
  "계정 자체 사망(탈퇴)"은 스냅샷으로 분리할 수 없다.
- **랭킹 깊이 교란**: 코호트는 레벨대뿐 아니라 랭킹 깊이(≈rank 4.2M vs 0.5M)·전투력(170배)이 함께
  다르다. "레벨대 효과"와 "성장도 효과"를 완전히 분리하지 못한다.
- **버닝추정군 부재 / BEYOND 폭주 미관측**: `approach`·`at260`에서 버닝추정 캐릭터가 0명이라
  "버닝추정 vs 비버닝 정착률 비교"와 Fisher's exact test를 수행하지 못했다. `climb` 에서도 270+
  폭주가 0명이라 "BEYOND 지정 캐릭터의 260→280 궤적"은 직접 관측하지 못했다 — 표본이 BEYOND
  미지정인지, 폭주가 35일 창 밖에서 일어나는지 구분할 수 없다.
- **`climb` 모멘텀 스크리닝**: `climb` 은 앵커 직전 14일 레벨 상승(2026-06-04 대비 ≥1)으로 표본을
  걸렀다. 이 창이 짧아(14일) 천천히 오르는 등반자는 놓칠 수 있고, 스크리닝 수락률은 17.5%였다.
- **이벤트 순효과 분리 약함**: 5/1(비수기)과 6/18(성수기)의 앵커일 랭킹 레벨 분포가 거의 같아
  비교 대비 효과가 작다고 판단, 단일 앵커(6/18)만 수집했다. 절대적 이벤트 효과의 반사실은 없다.
- **관측 35일**: 30일 이후 장기 추세는 이 분석 범위 밖이다(계획문서가 25→35일로 확대했으나 여전히
  이벤트 종료 9/16 이전까지만).
- **일간 `retention_rate` 빗살**: 전투력이 8일 간격 관측이라 일간 리텐션 컬럼은 D+8/16/24/32에만
  값이 튄다. 레벨대 비교는 "8일 구간 활동률"로 대체했다.
- **생존자편향 잔존**: 앵커일 랭킹에 진입조차 못 한 초저활동 유저, 그리고 앵커일 이후 개명·삭제된
  캐릭터(코호트별 1.4~5.7%)는 표본에서 빠진다.
- **분석 범위**: 레벨 200~300으로 한정. 200 미만은 API 페이지네이션으로 도달 불가.

---

## 12. AI 사용 로그

| 단계 | 사용 작업 | 사용 이유 | 검증 방법 |
|---|---|---|---|
| 설계 | 코호트 재정의(랭킹 실측 후), 관측창·표본 크기 결정 | 대안 탐색 — page↔레벨 매핑 실측치를 바탕으로 여러 코호트 구성을 비교 | 랭킹 API를 직접 60여 건 호출해 page↔레벨을 실측, AI 제안과 대조 후 채택 |
| 수집 | `collect.py` 작성 (재시도·레이트리밋·재개 로직) | 시간 절감 — 반복적인 API 클라이언트·CSV 재개 코드 | `test_collect.py` 순수 헬퍼 9개 자체 점검 통과, 실수집 중 락 에러 시 재시도 가드 추가 후 재실행 |
| 수집 | 수집 중 부분 데이터 이상 신호 조사 (완전 정지 계정 다수) | 검증 — "데이터 문제인가 실제 신호인가" 판별 | 소급 API가 날짜별 실제 값을 주는지 별도 확인(움직이는 캐릭터 6명 존재, 요일별 분포 상이 → 아티팩트 아님) |
| 분석 | `analysis.ipynb` 셀 구성·지표 계산·시각화 | 시간 절감 — pandas/scipy/matplotlib 보일러플레이트 | 각 지표를 원자료로 역산 검증(돌파자 수, 정착 k/j, 정체 분포), 6개 이미지 육안 확인 후 축·스케일 수정 |
| 재설계 | 초기 분석이 "다 정지"만 나온 원인 진단 → `climb`(모멘텀 스크리닝) 코호트 추가 수집 | 검증 — 기존 코호트 설계가 등반자를 배제한다는 가설을 검정 | `climb` 260 도달률 18% vs `approach` 0.3% 로 스크리닝 유효성 확인, 도달자 거동을 원자료로 분류 |
| 해석 | 유니온·버닝 BEYOND 메커니즘을 반영한 재해석 | 대안 탐색 — 도메인 지식(유저 제공 블로그·커뮤니티 자료)을 데이터 패턴과 대조 | `exp_rate`·전투력 분포, `climb` 260 도달자의 정지 레벨(261 아닌 정확히 260)을 수치로 확인. BEYOND 지정 규칙은 인벤·나무위키로 교차 확인 |
| 문서 | 계획·스펙 문서 최신화(설계 변경 3·4), 이 리포트 | 시간 절감 | 수치는 노트북 실행 출력에서 직접 인용, 한계점은 측정 가능/불가능을 구분해 명시 |

---

## 13. 부록 · 재현 방법

```bash
pip install -r requirements.txt

# 환경변수 (소스·CSV에 키 하드코딩 금지)
#   Windows PowerShell:  $env:NXOPEN_API_KEY = "live_..."
#   또는 .env 파일 (.env.example 참고, .gitignore 처리됨)

python collect.py          # data/cohort_{approach,at260,past260,burnend,climb}.csv 생성/이어쓰기
                           # climb 은 앵커 직전 14일 레벨 상승 스크리닝(결과는 cohort_climb_roster.txt 캐시)
                           # 앵커일이 과거 고정이라 실행일이 밀려도 데이터 동일, (캐릭터,날짜) 단위 재개

jupyter nbconvert --to notebook --execute --inplace analysis.ipynb
                           # data/breakthrough_retention_summary.csv, data/stagnation_pooled.csv
                           # images/01~06*.png 생성
```

- Python 3.10+ · 의존성: `requests pandas numpy scipy matplotlib jupyter` (버전 하한만).
- 데이터 출처 표기: **NEXON Open API**. 비상업적 포트폴리오 용도.
- 시각화: 01 돌파 곡선 / 02 8일 리텐션 / 03 정체 지도 / 04 성장 궤적 / 05 전투력 성장 분포 /
  06 climb 궤적.

---

## 14. 후속 분석 (Part 2) 방향

Part 1은 여기까지다: **260 허들 가설 → sampling bias 발견 → `climb` 코호트 추가 →
실제로 오르던 캐릭터도 260→261 전환율이 매우 낮다** (관측 기간 부족 아님, robustness check §6).
STOP 현상을 더 세분화하는 것은 Part 2의 목적이 아니다.

### Business Question (Part 2)

> **"방학 성장 이벤트에서 성장한 캐릭터 중 어떤 캐릭터가 260 이후 상시 progression으로 전환되며,
> 그 차이를 조기에 식별할 수 있다면 어떤 개입으로 Seasonal User를 Core User로 전환할 수 있는가?"**

단순 "STOP vs CONTINUE" 이분법 대신 **progression conversion** 문제로 본다. 260에 남은 캐릭터
중에도 `exp_rate`가 느리게 진행되는 경우가 있어, 활동/비활동 이분법보다 전환 퍼널이 정확하다.

```
하이퍼 버닝 / Seasonal Growth
  → Lv.260 도달
  → Post-260 Progression Conversion        ← 핵심 전환 지점
  → 6차 / HEXA / Symbol / Equipment / Boss progression
  → Persistent Growth
  → Event-end Retention
```

### 접근 (설계만, 대량 수집 전)

- **제약**: 랭킹은 ~4개월만 소급 → 2025 시즌 코호트 신규 추출 불가. 캐릭터 endpoint는
  2023-12-22까지 소급 가능 (`endpoint_feasibility_probe.md`). → 기존 1,958 OCID의 look-back
  패널로만 시즌 비교 가능. 2026-06 이전 완전 이탈 계정은 관측 불가.
- **패널**: `collect_panel.py` + `panel_config.py` — ASSEMBLE(2025 여름) / CROWN(2025 겨울) /
  OVERDRIVE(2026 여름) 시점의 레벨·투자 스냅샷. 상태 후보 = Seasonal Parker / Persistent
  Grower / Reactivated Grower / Dormant (분류 규칙은 raw 패널 분포를 본 뒤 확정, 코드에 미고정).
- **시간순서 엄수**: predictor는 pre-event 시점(예: 2026-06-12) feature만. outcome(2026-07+
  성장) 이후 형성된 HEXA/장비는 predictor로 쓰지 않는다. "280까지 큰 캐릭터는 HEXA가 높다"는
  설명적 결과이지 leading indicator가 아니다.
- Leading indicator 분석은 **Persistent Grower 표본이 충분히 확보된 뒤** 수행 (현재 `climb`의
  전환 캐릭터 n=5로는 모델링하지 않는다).
